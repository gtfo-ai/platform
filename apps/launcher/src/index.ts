/**
 * Workspace provider service (dockerode).
 *
 * Scaffold marker only: later work packages replace this with the real exports.
 * It exists so the package resolves, typechecks and ships a green suite.
 */
export const packageId = '@platform/launcher' as const;
