#!/usr/bin/env node
/**
 * `pnpm dev` — server + web with the fake Claude runner (CLAUDE.md).
 * Stub until the composition roots exist: apps/server lands in WP-06,
 * apps/web in WP-20. Exits 0 so the scaffold stays green.
 */
process.stdout.write(
  [
    'pnpm dev is not implemented yet.',
    '  apps/server  — WP-06 (Fastify skeleton)',
    '  apps/web     — WP-20 (SPA foundation)',
    'See docs/technical/13-implementation-plan.md.',
    '',
  ].join('\n'),
);
