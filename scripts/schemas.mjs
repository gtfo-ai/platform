#!/usr/bin/env node
/**
 * `pnpm schemas` — regenerate the published JSON Schemas under schemas/ from the
 * zod definitions in packages/contracts (TD-019, technical/12).
 * Stub until packages/contracts lands in WP-01. Exits 0 so the scaffold stays green.
 */
process.stdout.write('pnpm schemas is not implemented until WP-01 (packages/contracts).\n');
