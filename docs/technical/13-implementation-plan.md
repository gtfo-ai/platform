# 13 — Implementation plan (handoff to the implementation orchestrator)

> This is the work breakdown for a Claude Code orchestrator and its agent team. Product scope: product/14 (v0.1 = M1 loop, M2 trust, M3 show-the-value). Every work package (WP) lists inputs, outputs, acceptance criteria and dependencies. The orchestrator should run WPs as tickets through its own process: plan → implement → review (fresh context) → tests green → merge.

## How the work is run
See `14-orchestration-protocol.md` (orchestrator loop, fresh-context implementer and reviewer subagents, ledger `PROGRESS.md`, commit/push policy).

## Ground rules for the implementer
1. Read `docs/README.md`, all `docs/product/*`, all decisions, then `docs/technical/*`. The docs are the spec; product questions go to `docs/OPEN-QUESTIONS.md`, not into code comments.
2. Follow the code structure in technical/01; dependency rule `domain ← application ← infrastructure ← apps`.
3. Tests per technical/10 are part of each WP's definition of done; contract tests for every integration port; golden fixtures for SDK streams.
4. No secrets anywhere; `.env.example` maintained with every new variable; gitleaks pre-commit.
5. Conventional commits with `Signed-off-by`; small MRs; each WP ends with docs updates (CHANGELOG via release-please, ADR if a decision changed).
6. Install `docs/technical/CLAUDE.md.proposed` as `CLAUDE.md` at repository root in WP-00 and keep it under 200 lines.

## Milestone M1 — the loop

| WP | Title | Depends on | Acceptance criteria |
|---|---|---|---|
| WP-00 | Repo scaffold: pnpm monorepo, TS strict, Biome, Vitest 5 projects, lefthook, commitlint, CI `ci.yml`, CLAUDE.md, CONTRIBUTING, licence files, `.env.example`, Renovate, gitleaks | — | `pnpm test` runs empty suites green in CI; secret scan active; DCO check |
| WP-01 | `packages/contracts`: zod schemas for events, artifacts, `.agentic/config.yml` + `pipeline.yml` (technical/12), API DTOs, `TranscriptEvent`; JSON Schemas published under `schemas/` | WP-00 | Schema tests; `pnpm schemas` regenerates; unknown keys rejected |
| WP-02 | `packages/domain`: aggregates (Task, Run, Question, Approval, Budget, Project config merge), state machines, policies (iteration limits, WIP, budgets, autonomy presets, command policy evaluation), permissions `can()` | WP-01 | fast-check model tests for state machines; 90/85 coverage; exhaustive role×action test |
| WP-03 | Postgres schema + Drizzle + migrations (technical/03): partitions, event store, `handler_executions`, `inbox`, pg-boss install, price list seed, rollup tables; `migrate` entrypoint with advisory lock | WP-00 | Testcontainers migration test; partition auto-creation job; `REVOKE` verified |
| WP-04 | Event store + priority dispatcher + outbox job (TD-005) with `EventBus`, idempotency, chaining, `stop()`; Broadcast port (NOTIFY) | WP-02, WP-03 | property tests: at-least-once + idempotent handlers → exactly-once effects; ordering per stream |
| WP-05 | Jobs port on pg-boss: timers, cron, singleton/coalesced jobs; working-day calendar for question timeouts | WP-03 | timer accuracy tests; coalescing test |
| WP-06 | Fastify server skeleton (TD-002): config (zod), pino, Prometheus, health, Better Auth (email/password, sessions, admin), RBAC middleware, OpenAPI, SSE endpoint with topics and replay (TD-014), `ROLE` bootstrap, graceful shutdown | WP-04 | e2e: login, SSE replay after reconnect, shutdown drains SSE; OpenAPI generated |
| WP-07 | Integration ports + fakes + contract test suites for all five types (technical/06); `IntegrationActionExecutor` (shadow guard, idempotency, rate limits, audit) | WP-04 | contract suites green against fakes |
| WP-08 | Jira Cloud provider: thin client, ADF conversion, webhook verification + normaliser, polling fallback, transitions with runtime resolution, workpad upsert, questions, setup guide, recorded fixtures | WP-07 | contract suite green in replay mode; webhook golden tests |
| WP-09 | GitLab provider (gitlab.com + self-managed): thin client, MR/discussions/pipelines/coverage/CODEOWNERS, webhooks (legacy + Standard Webhooks), token minting/revocation, protected branch check, `listMergedMergeRequests`, setup guide | WP-07 | contract suite in replay; token minting tested against fixtures |
| WP-10 | Slack provider: Bolt Socket Mode, task threads, question/approval Block Kit, answer capture, identity mapping, digest job | WP-07 | contract suite; manifest file |
| WP-11 | Sentry + Loki providers: thin clients, pre-fetch for bug tasks, agent tooling specs (sentry-mcp/logcli), setup guides | WP-07 | contract suites |
| WP-12 | Claude SDK runner (technical/04): RunSpec → `query()` streaming input; hooks (policy, path guards, truncation, redaction, compaction markers); `canUseTool` → Question; platform MCP tools; session store; structured output validation; cost extraction; `ClaudeRunner` port + `FakeClaudeRunner` + fake `spawnClaudeCodeProcess`; transcript normaliser → `TranscriptEvent` with golden fixtures | WP-04, WP-05 | fixtures for all scenarios; redaction tests; budget-exceeded and stall handling tests |
| WP-13 | Run shim `agentic-runlet` (TD-025): frame protocol, single authenticated connection, spawn/stdio/signal/exit, `cred.get`, kill-on-disconnect; runner-side `SpawnedProcess` adapter; conformance tests (fake CLI, disconnect, signals, large stdout); verify `volume-subpath` on the target Docker version and embedded DNS on internal networks | WP-12 | SDK `query()` completes end-to-end through the shim in a hardened container in CI; disconnect kills the child within the grace period; report in `docs/research/12-run-shim-verification.md` |
| WP-14 | Launcher service + `WorkspaceProvider` (docker + fake): mirror, clone, hardening, sidecar, broker, export, retention; **`docker stop`/`rm` on every path that ends a run**, inherited from WP-13 — the shim's teardown signals one pid, so a *detached* grandchild survives it and only the container's pid namespace ends it (`docs/research/12-run-shim-verification.md` § "Teardown covers the child, not its descendants") | WP-13 | e2e in CI with a fixture repo container; security flags asserted; a run whose child forked a detached grandchild leaves **no** process behind, including on the paths where the shim exited first and the launcher is only tidying up |
| WP-14a | **The ten platform skills, and the provisioning step that mounts them.** WP-17's row lists "platform skills" and deliberately shipped none; **the refusal was correct** and the SDK fact that makes it correct is recorded so nobody re-litigates it: `skills?: string[] \| 'all'` (`@anthropic-ai/claude-agent-sdk@0.3.267/sdk.d.ts:2109`) is *"a context filter, not a sandbox: unlisted skills are hidden from…"*, matching the `SKILL.md` `name`/directory name — so `RunSpec.skills` can only **filter** what the CLI discovers in the workspace, and technical/04:35 is where skills get there (*"copy platform skills into the workspace `.claude/skills/_platform/` at provisioning"*). Provisioning is `WorkspaceProvider.create`, so this row belongs to WP-14 rather than to WP-17 or WP-22. The ten are product/13 § "Skills": `gitlab-mr`, `jira-ticket`, `loki-logs`, `sentry-issue`, `kb`, `ask-human`, `verify-work`, `mr-description`, `retro`, `file-followup-ticket`. The absence is already **declared** in the tree: `packages/prompts/` has no `skills/` directory, GitLab's registration names `packages/prompts/skills/gitlab` (`packages/integrations/src/providers/gitlab/index.ts:40`) — a path that does not exist — Loki's is `null` with a docblock saying why (`.../loki/provider.ts:195-197`), nothing reads `skillRefSchema` (`packages/application/src/ports/integrations/common.ts:254`), and the planner sends `skills: []` (`packages/application/src/pipeline/planner.ts:333`), which `options.ts:152` turns into *omitting* the option — per the same SDK docblock (`:2089-2098`) *"omitted … is **not** 'skills off'"*, so the platform applies no filter today and a project's own `.claude/skills` are enabled, which is product/13 § "Reuse of project skills" working as documented. PROGRESS backlog entry **24** | WP-14, WP-17 (both landed). **Not** blocked by Q52 for the first two criteria; the third is, and is scoped out below rather than written as a criterion nobody can run | the ten skills exist as `packages/prompts/skills/<name>/SKILL.md` whose frontmatter `name` equals the directory name (the SDK matches on exactly that), and GitLab's dangling `skill.path` resolves; provisioning copies them into the workspace's `.claude/skills/_platform/`, asserted **in a real container** by the WP-14 docker workspace e2e (`test/e2e/workspace/docker-workspace.e2e.test.ts`) — present on disk, the project's own `.claude/skills` untouched — and `RunSpec.skills` names exactly the platform skills the stage's role may use rather than staying `[]`; **explicitly out of scope**: any assertion that a model *lists* or *uses* them, which needs a production `ClaudeRunner` — which **no plan row composed until WP-15g** (Q52 is the transport question, not a work package; WP-22 builds images and compose, not the composition root), so a criterion about a live run would be unrunnable here by design and belongs to **WP-15g**, which this row's first two criteria do **not** wait on |
| WP-15 | Pipeline interpreter + stage executor + sagas (technical/02): templates feature/bug/chore, gates (CI, rebase, merged), returns, counters, escalation, questions/approvals, batching, budgets, WIP scheduler, workpad updates, status mapping | WP-04…WP-12 | fake-Claude e2e: one feature and one bug ticket through the whole loop; property tests on interpreter |
| WP-15a | **Compose the pipeline into `apps/server`** — the binding loader nobody's work package owned. `createPipelineRuntime` is called only by `packages/application/src/testing/pipeline-harness.ts`, and `PipelineIntegrations` has **no production constructor**, so nothing reads the `bindings` table (`integration_bindings` is prose; the tables are `integrations` and `bindings`) and instantiates the provider adapters. Scope: a loader that turns a project's `integration_bindings` rows into `PipelineIntegrations` (decrypting credentials through a `SecretStore` — **there was no existing broker**, which this row asserted and WP-15a disproved: nothing read `secrets.ciphertext` and nothing imported `node:crypto` for it, so WP-15a also built an AES-256-GCM envelope; `workspace/broker.ts` mints run-scoped git tokens and is a different thing — composing the per-run redactor Q55 describes), its registration in `apps/server/src/runtime.ts`, and the pipeline e2e moved onto a **real `apps/server` instance** as `CLAUDE.md` requires of the tier | WP-15 | a ticket reaches `task.completed` through an `apps/server` instance started by the e2e harness, with bindings loaded from the database rather than constructed in the test; **the fake-Claude e2e no longer builds its own runtime** |
| WP-15b | **Postgres `IntegrationAuditLog` + `IdempotencyStore`, and the migration `integration_actions` is missing.** BD-003's whole outbound-audit claim rests on a port WP-07 shipped and nothing implements, so `main.ts:18` and `scripts/dev.mjs:70` call `startRuntime()` with no audit log and `/readyz` is 503 for ever on `ROLE=all\|worker`. Migration `0007_cost.sql`'s table has no `project_id`, `redaction_count` or `attempts`, all three of which `IntegrationActionEntry` carries. The adapter appends `integration.action.performed` / `.failed` in the same transaction, which needs stream-sequence allocation outside a saga — the thing `NormalisedEvent` stops short of on purpose. Amend technical/03 with the columns | WP-15a | a forward-only numbered migration adds the three columns and the Drizzle parity test holds them; `startRuntime()` composes a real audit log with **no caller supplying one**, and `/readyz` reads ok on `ROLE=all`; an action whose redactor fired records a non-zero `redaction_count` and one whose redactor was told nothing records **zero** — asserted in both directions, because that column is the only signal that a row which should have hidden something did not |
| WP-15c | **Webhook ingress and the `inbox`** — `apps/server/src/routes/` has no `/webhooks/<provider>/<integrationId>` endpoint (technical/06 § "Inbound") and no polling fallback, so nothing in production emits `ticket.matched` even once WP-15b lands, and `inbox` is unwritten for the same reason. Carries the **inbound redaction step** `docs/TODO.md` has open — `IgnoredDelivery.detail` and normalised payloads reach `events.payload` while TD-012 requires the redactor on every write to it — and records `redaction_count` on the inbox row. Unblocks the `KnowledgeIndexer` job (WP-18) **Architect ruling, recorded in PROGRESS under "Architect ruling (WP-15c)"**: the delivery key stays the adapter's **redacted plaintext** key — the one-way digest is **closed, not deferred** — because (a)'s safety property is a post-condition of the redactor and holds on every binding unconditionally, while a digest's weakness is conditional on deployment configuration an operator cannot see. The ruling struck the ledger's claim that `APP_SECRET_KEY` is unavailable (`config.ts:353`, `pipeline.ts:269` already derives it) and, more importantly, found that **the question was spent on the one column already guaranteed clean**: `inbox(headers, payload)` has existed unredacted since `0005_events.sql:113`, TD-012's write list does **not** name `inbox`, and **GitLab's legacy scheme sends the binding's webhook secret as plaintext in `X-Gitlab-Token`** — so an ingress storing the raw delivery writes a live credential to the database on every delivery, with nothing planted. `inbox` also has no `redaction_count` column, so this WP owes migration **0014**. | WP-15b, WP-08, WP-09 | a signed provider delivery to a running `apps/server` instance appends `ticket.matched` and drives a task to `task.completed` **without a seeded row**; a replayed delivery is deduplicated by `inbox(provider, delivery_id)` and performs nothing twice; an unverifiable signature is refused and audited; a binding credential planted in the delivery body reaches neither `events.payload` nor the inbox row; **and a matched ticket that has no task row is re-emitted, not merely detected** — since WP-15d the intake handler writes nothing and the task is created by the `pipeline.outbound` job it enqueues through `afterCommit`, which is at-most-once (TD-004), so a crash between the two leaves a matched ticket that nothing re-emits, nothing retries and nothing logs (PROGRESS backlog entry 20 has the evidence: no poller exists — `ports/integrations/task-management.ts:157` is the only mention — and `event-bus.ts:377-392` logs only a callback that *threw*). This is **compatible with the dedup criterion above because the two name different units**: dedup is about a **delivery**, recovery is about a **task**. So the reconciliation finds matched tickets with no task row and appends a **new** `ticket.matched`, leaving `inbox(provider, delivery_id)` untouched — re-dispatching the *old* event is **not** an option, because `handlerExecutions.claim`/`complete` (`event-bus.ts:351`/`:370`) commit in the handler's own transaction and a replay of that position is skipped by design. Safe to run blind: intake is idempotent on `tasks_project_id_ticket_key_mode` (`saga.ts:204`). Asserted by dropping the `afterCommit` callback and showing the ticket still reaches `task.completed` **and** `inbox.headers`/`payload` are stored `redactJson`-redacted, applied **after** `verify` and **after** the key is computed, with the signature verdict **persisted** because a redacted payload can no longer be re-verified; `redaction_count` on the row is the **summed** count over all three redactions, not the key's alone, which is ~always 0 and would be a dead signal; asserted live by a GitLab **legacy-token** delivery whose stored `inbox.headers` does not contain `webhook_secret_token` and whose `redaction_count >= 1`; and `packages/integrations/src/providers/delivery-key-redaction.test.ts` is left **unchanged**, because `expect(key).toContain(MARKER)` is the half that fails both when an adapter forgets to redact and when a new provider keys a field its recipe does not plant into |
| WP-15d | **Outbound provider calls move out of the handlers' transactions.** Three pipeline handlers call a provider while `context.scope.tx` is open — the intake default-branch read (`saga.ts:215` → `:261`/`:265`, in `pipeline.intake` at **priority 10**, the core band), the workpad upsert (`workpad.ts:168`) and the status transition (`workpad.ts:218`) — so a pooled connection is held across provider latency and the audit write nests inside the caller's transaction (`postgres-unit-of-work.ts:55` takes a second `pool.connect()`). This contradicts CLAUDE.md's stage shape (*transaction / no transaction / transaction*) and technical/06 § "Outbound: actions" (line 370: actions are triggered by handlers **in the integrations band 100–199**). It is the **cause** of two things WP-15b met as symptoms: `integration_actions_task_id_fkey`, which made every e2e die on its first event because the audit commits before the saga does (the FK was **correctly** dropped and is not reopened here); and the pool arithmetic — `POOL_RESERVATIONS.pipeline = 2` (`config.ts:220`) missed the nested connection, so a dispatch peaks at **3** against `CONNECTIONS_PER_DISPATCH = 2`, floor `2N+8` against a worst case of `3N+8`, and at the shipped default (N=1, `APP_DB_POOL_MAX=10`) **the floor equalled the default with zero slack**. WP-15b fixes the arithmetic (`auditPerDispatch`), which is the accommodation; this row removes the shape. Mechanism: `HandlerContext.afterCommit` + a job that re-validates on fire (TD-004, TD-005) — **not** a bare callback, which is at-most-once. Amend technical/02 and technical/06 to state the transaction an outbound action runs in. **Consequence under load is a hypothesis, not a measurement** (pool exhaustion / audit-write timeouts at concurrent intake); PROGRESS backlog entry 17 has the evidence | WP-15b; land **before or with WP-15c**, which is what first makes it live | no pipeline handler reaches a provider while a transaction scope is open, **refused mechanically** at `PipelineIntegrationsPort.forProject` so the next handler to try it fails a test rather than a production pool; the intake branch check, the workpad render and the status transition each survive a rolled-back handler transaction without having called the provider, asserted per site; a task whose commit succeeded and whose wake-up was lost still reaches the provider on the job's next fire; `POOL_RESERVATIONS.auditPerDispatch` is **0** and the pool floor is back to `2N + …`; and the exhaustion hypothesis is **measured** — N concurrent intakes against the shipped default, with the provider latency and the load stated (rule 64) — before the move as well as after |
| WP-15e | **Whole-row `tasks.save` loses a concurrent writer's update — the class, not the instance.** `TaskRepository.save` (`packages/application/src/pipeline/store.ts:80`) writes the **whole** row: correct for a saga step, and wrong the moment a writer exists outside the pipeline's own ordering — which WP-15d created in the ordinary course of moving a provider call out of a transaction, by rendering the workpad in a `pipeline.outbound` job that runs beside the stage executor's transactions. **Measured, not reasoned**: *"a bug ticket walked all seven agent stages and finished with `cost_actual` 2.40 instead of 2.80, because the workpad's whole-row write landed between the executor's read and its write and put a stale cost back"*, and in the same first `verify:e2e` after the move a feature ticket *"sat at `ci_gate` until the 90 s settle gave up"* — one cause, and the second symptom is the one that generalises: the loss reaches columns the racing writer never meant to touch, so a task simply stops moving with no error anywhere. WP-15d fixed **one** writer with a narrow port method (`saveWorkpad`, `store.ts:97`, one column, pinned by the contract case *"writes the workpad without writing anything else, so a concurrent cost survives"*); **twenty** production `tasks.save` call sites remain (`stage-executor.ts` 4, `transitions.ts` 5, `saga.ts` 11 — counted by grep, not measured for concurrency). Two shapes, and the choice is this row's to make rather than to assume: a **narrow write per writer** (cheap, no migration, does not compose — every new writer needs a reviewer to notice it needs one) or **optimistic concurrency on the row** (a version column, a refusal, a retry — catches the writer nobody anticipated, costs a migration and an error path per site). It is a silent data-corruption class whose first instance was found only because a cost happened to be summed, and what it corrupted is the platform's own record of spend. PROGRESS backlog entry 18 has the evidence | WP-15d; **before** any further work package moves a task write into a job | a `save` that lands on a row another transaction has moved since it was read **cannot silently win** — either it writes only the column it owns, or it is refused and the caller retries — asserted by a test that reproduces the interleaving **deliberately** (a job writing a task row beside a stage-executor transaction; WP-15d's shape, no load needed, rule 66); `cost_actual` **and** `stage` each survive a concurrent writer, asserted per column because the two observed symptoms were different columns; and every remaining `tasks.save` call site is either converted or covered by the refusal, with the count stated in the change rather than left to a reader to recount (rule 63) |
| WP-15f | **The ticket's own words — the platform reads no ticket text, so the first agent stage is given a key and a URL.** `tasks` stores `ticket_provider`/`ticket_key`/`ticket_url` (`0004`:6-8) and nothing else about the ticket, `ticketRefSchema` is `{provider, key, url}`, `ticket.matched` carries no title, and `TaskManagementPort.readTicket` (`packages/application/src/ports/integrations/task-management.ts:179`) — which already returns `title`, `description`, `comments[]`, `epic`, `siblings` and `attachments_text` (`:76-95`) — has **no production caller**. So the two things built to consume the ticket's content get its identity instead: the prompt's task block is three lines, `provider:`/`key:`/`url:` (`packages/domain/src/prompt/assembly.ts:342-351`), against technical/04 § "Prompt assembly" step 5's *"Task block: **the ticket**, artifacts, return feedback…"*; and the retrieval query at the first agent stage is the ticket key alone — `extractQueryTerms('ACME-1') -> ["acme"]`, `extractQueryTerms('PROJ-1234') -> ["proj","1234"]` — against technical/07:11's *"task text (ticket + spec)"*. **The documents are right and the code is behind them**, so nothing is amended here; this is work. Measured at WP-17 and confirmed by its reviewer independently of the implementer that reported it; PROGRESS backlog entry **23** has the evidence, and **Q61** is the product half (where the text lives) with a recommendation strong enough to build from: a bounded, redacted `ticket_snapshot jsonb` on `tasks` beside the `template_snapshot` precedent, fetched once at intake through a **fourth `pipeline.outbound` duty** beside `intake_check`/`workpad`/`status` (`packages/application/src/pipeline/jobs.ts:69`) so the call is outside every transaction and through `IntegrationActionExecutor` (WP-15d). **Nothing in the repository can currently fail because of this** — `FakeClaudeRunner` picks its scenario from `spec.stage` (`packages/infrastructure/src/runner/fake-claude-runner.ts:96`) and never reads the prompt — which is why it survived to WP-17 and why this row's acceptance is written against the *assembled prompt* rather than against a run's output | WP-15c, WP-15d, WP-17 (all landed); **Q61** for the storage shape; land it **before** the first real-model run, because it will otherwise be read as a prompt-quality problem | the assembled prompt at the **first** agent stage contains the ticket's title and description **inside** the `kind="ticket"` data block, asserted against the assembled prompt and not against the pack, so WP-17's delimiter contract is held at the same time; `taskTextOf` (`packages/application/src/pipeline/planner.ts:190`) yields the ticket's own words — stated as *"a ticket titled 'rollback sessions after a failed migration' produces a query containing `rollback`, `sessions`, `migration`"* and **never** as a relevance claim, because the fixture vault cannot falsify one (backlog 16); the snapshot is fetched by a `pipeline.outbound` duty and written by a **narrow** repository method — **no new `tasks.save` call site**, because a job writing a task row beside the stage executor is exactly backlog entry 18's interleaving and this must not become its twenty-first instance; the snapshot is **bounded and redacted** with the cut declared on the row (a `truncated` marker) and `redaction_count` recorded, and the byte budget is **stated in the change** rather than left to a reader to infer (rule 63, and Q54's *bound at the consumer* — here the store is the consumer); a manual start (`POST /api/projects/:id/tasks`, which writes no `inbox` row) gets a snapshot by the same path as a webhook-started task, asserted; and a ticket whose title carries a zero-width character is still retrievable by that word, or the residual is **stated at the line** (backlog entry 12's measured half: `extractQueryTerms('sess<U+200B>ions rollback') -> ["sess","ions","rollback"]`) |
| WP-15g | **Compose a production `ClaudeRunner` and the run's workspace into `apps/server` — WP-15a's shape a second time, and no plan row owned it.** `apps/server/src/pipeline.ts:430` composes `composition.runner?.(platformTools) ?? unavailableClaudeRunner()`, whose `start` **throws** `RunnerUnavailableError` (`:113-115`), so the first real ticket to reach an agent stage fails its run and escalates the task (Q59). Every green result in this repository is green through `FakeClaudeRunner`, which picks its scenario from `spec.stage` (`packages/infrastructure/src/runner/fake-claude-runner.ts:96`) and never reads the prompt — **standing rule 82**, which is why the acceptance below is written the way it is. **Q52 was checked rather than assumed and is not a blocker**: its recommendation — *"build the launcher as a plain object and compose it in-process for now"* — is implemented (`apps/launcher/src/service.ts`, `runtime.ts`: `buildLauncher` returns `{config, service, provider, logger, stop}`), and its two open sub-decisions (Unix socket versus HTTP; one interface versus a narrower remote surface) are both about the **out-of-process** transport, so neither blocks an in-process composition. **It is not pure wiring, and four collaborators have no production implementation at all** — said plainly so nobody scopes this as an afternoon: (1) **nothing builds a `WorkspaceSpec`** — the only producer in the tree is the test fixture `packages/infrastructure/src/workspace/fixtures.ts:38`, and `agenticConfigSchema` (`packages/contracts/src/config.ts:219`) has **no** workspace section, so limits, the egress allow-list (technical/05 § "Network policy": the model provider host, the git host, the registries discovery found), `runtime`, `readOnly` and `keepUntil` must be derived from instance config and the git binding under BD-025's narrow-never-widen rule; (2) **the runner has to be built per run** — `ClaudeRunnerDependencies.spawnClaudeCodeProcess` is a construction-time dependency (`packages/infrastructure/src/runner/claude-runner.ts:140`) while the socket path and token are per run (`WorkspaceAttachment`, `packages/application/src/ports/workspace.ts:188`), and `ClaudeRunner.start` is **synchronous** (`ports/runner.ts:431`) while `create`/`attach` are not; the shape to copy is WP-15a's loader, which already builds adapters **per call** for the same reason (Q55); (3) **no production `RunTranscriptSink` exists** — `run_messages` has existed since `0006_transcripts.sql` and nothing under `packages/` or `apps/` writes or reads it (grep finds only the contracts DTOs and docblocks), and the e2e passes `sink: { append: async () => {} }` (`test/e2e/support/pipeline.ts:433`), so a real run today produces a transcript nothing stores and no screen shows; (4) **no production `ToolApprovalPort`** — `apps/server/src/platform-tools.ts`'s own docblock names what is missing (*"the Question aggregate's HTTP surface and a waiter for the human's answer, the transcript sink, the task read model"*) and eight of the nine platform tools are named refusals. Two further facts this row owns. `@platform/launcher` exports **only** `./src/index.ts`, a `#!/usr/bin/env node` entrypoint with a top-level `await main()`, so importing the package in order to compose it **starts a launcher**: the service needs an export of its own (or `LauncherService` moves into `packages/infrastructure`). And TD-021 says *"Launcher service (`ROLE=launcher`) is the only component that can reach the Docker socket"*, which an in-process composition inside `apps/server` contradicts — **docs first: amend TD-021** with the single-process mode and what it trades (the server process gains Docker access), or declare a deployment mode that keeps the socket where TD-021 put it, **before** the code. Finally, `apps/server/src/role.ts:52` still reports `runner (WP-12: Claude SDK runner)` unimplemented although WP-12 is DONE — a stale attribution this row makes true. PROGRESS backlog entry **24**'s closing paragraph is where the gap was first stated without a row | WP-12, WP-13, WP-14, WP-15a, WP-15b, WP-17 (all landed). **Not** blocked by Q52's two sub-decisions. **Blocks WP-18**: its own row defers `KnowledgeIndexer` because it *"needs a checkout to read, which needs the workspace provider"*, and that dependency holds — with **one residual this row does not discharge**, so WP-18 should not assume it: `createFilesystemVaultSource` takes *"an absolute path of the checkout"* on the **server's own** filesystem (`packages/infrastructure/src/knowledge/filesystem-vault.ts:47-48`), while a workspace checkout lives on the `ws-<run>` volume inside a container and the mirror on `repo-cache`, so WP-18 still has to say where its readable checkout comes from. **Blocks WP-14a's third tier only** — its first two (the ten `SKILL.md` files, and provisioning copying them into `.claude/skills/_platform/`) need no runner and may land before, with or after this row. A *live* container run additionally needs WP-22's `platform-runtime` image, which does not exist (`test/e2e/workspace/docker-workspace.e2e.test.ts:24`) and is scoped out below rather than written as a criterion nobody can run | **No criterion here may be satisfiable by `FakeClaudeRunner`, and none may need a model credential** — there is none (no repository secrets, no environments, `ANTHROPIC_API_KEY` unset: the blocker brief in PROGRESS), so *"a ticket reaches `task.completed`"* is **not** a criterion for this row, because it already passes. (1) **The real runner, in the pipeline e2e, over the fake CLI**: the tier drives a ticket with `createClaudeRunner` — the real adapter over the SDK's `query` — whose `spawnClaudeCodeProcess` is WP-12's `fakeSpawnClaudeCodeProcess`, and the assertion is that **the ticket's own title reaches the process**, asserted against what the CLI *received* (`FakeCli.stdin`, *"every frame the SDK wrote to stdin, parsed"*, and/or `FakeCli.spawnOptions`) and **never** against the `RunSpec` the planner built, since a spec assertion is exactly the one the fake runner already cannot fail; **needs measurement** (rule 66, not run here): which of stdin and argv carries the system-prompt append, stated in the change rather than assumed. **What it does not prove**: that the real `claude` binary accepts those options, that the container, the shim, the credential socket or the egress policy work, or anything whatsoever about the model's output. (2) **The real transport, once, without a model**: one run through `LauncherService` + `DockerWorkspaceProvider` + `createRunletSpawn` + the real shim against a **fake CLI** — which has never happened: `packages/infrastructure/src/runlet/conformance.contract.test.ts` completes SDK `query()` through the real shim over a real Unix socket, but with the SDK's own `query` and no launcher-created workspace. It needs a daemon, so it is **not** a `verify` target: it lands beside `node scripts/runlet-container-check.mjs` and WP-14's docker workspace e2e, and the row says so rather than adding a tier CI cannot run. **Does not prove** the `platform-runtime` image or the real CLI (WP-22). (3) **The transcript is stored and redacted**: the run's `run_messages` rows are written by *this* composition's sink and are non-zero, and a secret planted in the run's `env` (`RunSpec.secretEnvNames`, TD-012 step 1) appears in **none** of them — asserted in both directions, the way WP-15b asserted `redaction_count`, because a sink that redacted nothing and a sink that had nothing to redact look identical from one side. (4) **Every ending frees the workspace** — completed, failed, stalled, budget-exceeded, cancelled — asserted **per ending** rather than once, because WP-13's teardown finding is that a run container outlives anything that signals one pid, and `LauncherService.endRun` discharges it only if something calls it on every path. (5) **Q59(b), decided on this row**: `unavailableClaudeRunner` **stays** and becomes conditional on *configuration* rather than on the build — a process with no launcher configuration composes the refusal, logs which piece is missing (the shape `startRuntime` already uses for the runner and the audit log) and still runs the gates, the status mapping and the workpad; refusing to compose the pipeline at all would be louder and would stop the part of the loop that works without an agent. Asserted by a process started with no launcher configuration reading `/readyz` ok and still moving a non-agent stage. (6) **Q59(a)**: a `start` that fails for a **transport** reason is distinguishable from one that fails terminally, and the row states which escalates and what bounds the retry — one flap must not escalate a task, and an unbounded retry must not hide a dead launcher. (7) `ROLE=runner` no longer reports itself unimplemented and `PENDING_WORK_PACKAGE` no longer names WP-12 for it (`apps/server/src/role.ts:51-52`) |
| WP-15h | **The read API the SPA already calls, and the `run:<id>` SSE topic nothing publishes — no row ever owned the surface between WP-06's skeleton and WP-20's screens.** `apps/web/src/api/endpoints.ts` calls **20** distinct `/api/*` paths; `apps/server` registers **four** of them (`/api/version` `routes/ops.ts:137`, `/api/org/users` `org.ts:62`, `/api/org/audit` `org.ts:75`, `/api/projects/:project_id/config` `projects.ts:40`), and `apps/server/src/queries/` holds one file, `identity-queries.ts`. So every screen but sign-in, version, the audit list and one config read answers 404 against a real server, and WP-15g's `run_messages` rows — the first this repository ever wrote — are unreadable: `RunTranscriptSink`'s docblock says an entry goes to *"`run_messages` plus the `run:<id>` SSE topic"* (`packages/application/src/ports/runner.ts:256`), `apps/server/src/sse/hub.ts:4-5` already lists that topic, and **nothing publishes one**. It cannot be a `NOTIFY` payload (broadcasts are capped at 7 000 bytes and carry hints), so it is a read-back from the rows. Nothing caught this because the only tier that exercises the client's endpoint list answers it with a **fake API backend** (WP-20's own criterion, *"Playwright e2e with fake SSE"*). PROGRESS backlog entry **29** has the census. **Scope may be split by screen**; the run endpoints go first, because they are the only ones with a producer already writing rows. **It was split, and only the first half is merged**: **part 1** — `GET /api/runs/:run_id` and its `/messages`, `/prompt`, `/context-pack`, `GET /api/tasks/:task_id`, the `run:<id>` publisher and both censuses — landed at `19da103`; **part 2 is open** and is the **seven** remaining reads the census attributes to a later iteration of this row (the running-agents, inbox, integrations, setup-guide, projects, readiness and task-list screens) **plus one read the census cannot see**: a `kb/health` reader for `kb_health_reports`, which WP-18b writes and no client calls (PROGRESS backlog **37**) — the census is client-driven, so this one is a criterion stated here rather than a gap it would find. **Part 2 landed in session 5** and closed the seven reads plus `kb/health`; `budgets` landed with WP-19 and the three `kb/*` reads with WP-18b, so **every read the SPA calls is served**. What was still unserved after part 2 was the task and run **command** surface — twelve writes, of which **WP-15i** served eleven and `steer` stays WP-27's — a different row again because each needs the aggregate, a `human_actions` row and an `Idempotency-Key`; and this row's own second half, serving the SPA (backlog 33). **Both are rows now** (refiner, session 5): the commands are **WP-15i** (eleven of the twelve; `/api/runs/:run_id/steer` stays WP-27's) and serving the SPA is **WP-15j**, because part 2 merged without that half. None of them is restated here: `apps/server/src/routes/client-census.test.ts` holds each one in `ADMITTED_GAPS` with the row that owns it and fails in **both** directions, so a copy in this document is a second list to keep true. **Part 2 was to own serving the SPA itself, and it merged without it — that half is now WP-15j below** (PROGRESS backlog **33**, added by the refiner in session 5): technical/09:6 says the bundle is served as a static bundle by the app process with SPA fallback on the same origin as the API and SSE, WP-22 put the built `apps/web/dist` into the product image, and **no route serves it** — there is no `@fastify/static`, no `sendFile`, and the only `setNotFoundHandler` answers JSON, so a `docker compose up` instance ships the SPA and answers 404 for it. Acceptance for that half: an unknown **client** path answers the shell, an unknown `/api/*` path still answers a JSON 404, `/events` is untouched, and the fallback rule is **allow-list shaped rather than a deny-list of API prefixes** (rule 55), so a route added later by someone who never reads the fallback cannot silently start returning `index.html` | WP-06, WP-15g, WP-20 (all landed). **Not** blocked by Q52 or WP-22 — Node and Postgres only, no daemon | `GET /api/runs/:run_id/messages` returns the rows **this repository's own pipeline wrote** — asserted in the e2e tier by driving a fake-Claude run through `apps/server` and then reading the endpoint, never against a seeded table — cursor-paginated, snake_case, against a **strict** DTO from `packages/contracts`, in `seq` order including `seq: 0` (migration 0016); the `run:<id>` SSE topic carries a frame for every appended entry with the **publisher owning `seq`** (`sse/hub.ts:22`) and that number being the row's `seq` rather than a second numbering, so a client that reconnects with a cursor replays positionally; **a secret planted in the run's `env` (`RunSpec.secretEnvNames`) appears in neither the response nor the frame, asserted in both directions** the way WP-15b asserted `redaction_count`, because a reader that redacted nothing and a reader that had nothing to redact look identical from one side; a row with a non-null `blob_id` is resolved or refused **explicitly** (PROGRESS backlog **7**'s blob line — nothing sets it today, so a reader must not be written against a column that is null by accident); every new route carries the same auth as the existing four, asserted **per route** by an unauthenticated request rather than once; and **the recurrence is closed, not just the instance**: a census test reads the client's paths off `apps/web/src/api/endpoints.ts` and the server's off its registered routes and **fails on a path the client names and the server does not serve**, with the endpoints this row leaves unimplemented listed in the test as an admitted gap rather than the check being scoped to the ones that pass — the shape `apps/launcher/src/docker-access.test.ts` uses |
| WP-15i | **The task and run command surface — the eleven writes the SPA calls and nothing serves.** Backlog 29's read half closed at WP-15h and its write half did not: `apps/server/src/routes/client-census.test.ts`'s `ADMITTED_GAPS` (`:63-83`) is now **commands only** — twelve paths, each excused as `'the task command surface — not this row'` (`:71`) or `'the run command surface — not this row'` (`:80`) — and the census fails in **both** directions, so a route that lands must leave the list. The twelve are `/api/tasks/:task_id/{pause,resume,cancel,retry-stage,return-to-stage,rework,feedback}`, `/api/tasks/:task_id/questions/:question_id/answer`, `/api/tasks/:task_id/approvals/:approval_id/decide` and `/api/runs/:run_id/{steer,retry,cancel}`. **One of them is M2's**: `POST /api/runs/:run_id/steer` is WP-27's deliverable (M2 below, *steer + take-over/hand-back*), so **eleven** are this row's. Not this row's either, and not in the census because the SPA does not call them: `POST /api/tasks/:task_id/ask` (WP-31, ask-the-task) and `POST /api/tasks/:task_id/{take-over,hand-back}` (WP-27), both named at technical/08:17. `…/approvals/:approval_id/decide` **is** this row's — the plan-approval gate is M1 (product/04:45, BD-006) and WP-28 adds the *budget* approval that flows through the same route rather than a second one. **The domain half is shipped and the HTTP half does not exist**: `pauseTask` (`packages/domain/src/aggregates/task.ts:407`), `cancelTask` (`:584`), `returnToStage` (`:281`), `resumeStage` (`:336`), `answerQuestion` (`packages/domain/src/aggregates/question.ts:156`) and `decideApproval` (`packages/domain/src/aggregates/approval.ts:110`) all exist, while the **whole server registers two POST routes**, `apps/server/src/routes/kb.ts:295` and `apps/server/src/routes/webhooks.ts:122`. **`human_actions` has no writer**: the table has existed since `packages/infrastructure/src/db/migrations/0004_pipeline.sql:210`, is registered `append_only` (`:221-222`) and is published as `humanActionRecordSchema` (`packages/contracts/src/records.ts:265`), and grep finds no insert under `packages/` or `apps/` — so technical/08:73's clause about every human action being recorded is vacuous today. **No request-level idempotency store exists**: outside the integrations executor the only key in the tree is a provider-action key (`packages/application/src/knowledge/apply.ts:335`), and WP-15b's store is scoped by `idempotencyScopeFor` to outbound provider calls — while the client already sends the header on **eight** of the twelve (`apps/web/src/api/endpoints.ts:177-188` threads `idempotent` through, and the comment at `:262-264` names the failure it exists for, `'A double-clicked Retry that starts two runs'`). **What it costs to leave**: the loop runs and a human cannot pause, cancel, answer a question or approve a plan from the product — every button on the task and run screens answers 404 against a real server. `POST /api/projects` and `POST /api/projects/:project_id/discovery` are **WP-21's**, not this row's (`apps/server/src/routes/projects.ts:17` says of them `'are writes and belong with the command surface'`); the census does not name them only because the wizard does not call them yet | WP-02, WP-06, WP-15, WP-15e, WP-15h (all landed). Node and Postgres only — **not** blocked by Q52 or WP-22. Land it **before WP-23** (a user guide for buttons that answer 404 documents a defect) and **before WP-27, WP-28 and WP-31**, each of which adds a command to this surface | **(1) The aggregate decides, per command.** Each of the eleven loads the aggregate and calls the domain function named above; a transition the state machine refuses is a **typed 409** whose code names the refusal, asserted per command from a state that refuses it (cancel on a cancelled task, answer on an answered question, decide on a decided approval) **and** from a state that admits it, because a route that refused everything would pass the first half on its own. **(2) One `human_actions` row per accepted command**, carrying the acting `user_id`, the command and its params, and **none** for a refused one — asserted in both directions the way WP-15b asserted `redaction_count`, and read back through a query in the same test rather than asserted as an insert, because a table nothing reads is how this one reached WP-15i with no writer. **(3) `Idempotency-Key` performs nothing twice**, asserted on a countable effect and not on a status code: the same key replayed leaves one `runs` row, one answer, one `human_actions` row and returns the first response, and the **same key with a different body** is refused rather than served the first result; the key's scope (user, route, resource) is stated in the change with the reasoning `idempotencyScopeFor` carries. Seven of the eleven are the ones the client already marks idempotent; for the other four (`pause`, `resume`, task `cancel`, run `cancel`) the row states whether the header is required and why a repeat is harmless. **(4) The same auth per route**, asserted per route: 401 `unauthenticated` to an anonymous caller (the census's auth half sweeps this the moment a route exists) **and** at least one wrong-role 403 per capability class, since the census probes only anonymity — capabilities from technical/08:69 (`task.answer_question: member`, `task.approve_plan: maintainer`). **(5) Asserted in the e2e tier against a real instance**: a fake-Claude task driven through an `apps/server` instance, the command issued over HTTP, the effect read back through WP-15h's read endpoints — never against a seeded table (rule 82). **(6) A command that lands beside a running stage cannot silently win** (WP-15e): every write is a narrow repository method or goes through `retryOnTaskConflict` (`packages/application/src/pipeline/task-conflict.ts`), and an **exhausted** conflict surfaces to the caller as a typed 409 rather than escalating the task to `needs_human` from an HTTP request; asserted by deliberately interleaving a command with a `stage.execute` transaction (WP-15e's shape, no load — rule 66), with `cost_actual` **and** `stage` both surviving. **(7) The census's gap list shrinks by exactly these eleven**, its one remaining entry names **WP-27**, and the eleven are named positively in a case of their own — the shape `apps/server/src/routes/client-census.test.ts` already uses in `serves the seven reads WP-15h part 2 took off the gap list` (rule 10). **(8)** Request bodies are the **strict** contracts the client already sends (`pauseTaskRequestSchema` and its siblings in `packages/contracts`), snake_case on the wire, an unknown key an error |
| WP-15j | **Nothing serves the SPA, and the product image has carried the bundle since WP-22 — no row owned the process that hands the app to a browser.** technical/09:6 reads `'Served as a static bundle by the app process with SPA fallback; same origin as the API and SSE.'` and technical/11:98 states the gap in the other direction, `'Nothing serves that bundle yet'`. There is **no `@fastify/static` and no `sendFile` in `apps/server`**, and the only `setNotFoundHandler` answers JSON (`apps/server/src/app.ts:217-219`). The packaging half is done: `docker/app.Dockerfile:100` copies the Vite output to `/app/apps/web/dist`, and its own comment at `:23` says *"Nothing in `apps/server` serves it"*. `compose.yml:125` publishes `${APP_PORT:-8080}:8080`, so `docker compose up` exposes a server that answers 404 at `/`. PROGRESS backlog **33** has the rest, including the allow-list-shaped fallback (rule 55). **What it costs to leave**: WP-22's own criterion is a working instance and the product's entire user surface is unreachable on one; WP-23's dogfood criterion — a stranger installing in under an hour — is unmeetable because the stranger has nothing to look at. Same origin is load-bearing rather than a nicety: the SSE client and the API client both assume it, so a stop-gap that serves the bundle from a second origin reopens the auth and CORS questions the design closed. No `APP_WEB_ROOT` was added at WP-22 on purpose — a variable nothing reads is rule 31's shape — so this row owns the variable as well as the route | WP-20, WP-22, WP-15h (all landed). Node only — no daemon for criteria 1–7, and **not** blocked by Q52. Land it **before WP-23**, whose dogfood criterion depends on it | **(1) The shell and the assets come off disk.** A real `apps/server` instance serves `index.html` at `/` and the hashed assets under the bundle's own directory, asserted against a directory holding a real `index.html` and a real asset with the **served bytes compared to the file's bytes**, so a route answering a hard-coded string cannot pass (rule 82). **(2) A deep link answers the shell and the API never does.** A client route (`/projects/<key>/tasks/<id>`, `/runs/<id>`) returns the shell, while `/api/*` — including an `/api/…` path **no route serves** — plus `/events`, `/webhooks/*`, `/healthz`, `/readyz` and `/metrics` are asserted **per prefix** never to fall back to it; and the fallback is **allow-list shaped rather than a deny-list of API prefixes** (rule 55), deciding that a request is the client's by construction, so a route added later by someone who never reads the fallback cannot silently start returning `index.html`. **(3) A path outside the bundle root is refused**, asserted with a **real file the test created outside the root** so a refusal cannot be confused with a missing file: `../` traversal, its percent-encoded spelling, an absolute path, and a **symlink inside the bundle directory pointing outside it** each answer 404 or 403 and no byte of the target is served (BD-022 — a static file server is a path guard). **(4) The census is unaffected and unchanged.** `apps/server/src/routes/client-census.test.ts` passes with no edit and the admitted gaps still probe as *not served*: the fallback must not make an unserved `/api/*` path look served, which would turn off the instrument that found this class (the census classifies by the not-found handler's own body, `client-census.test.ts:219-231`). **(5) One constant, not two.** The directory the image copies the bundle to and the directory the server reads are held equal by a check that reads `docker/app.Dockerfile` — the census shape `apps/launcher/src/docker-access.test.ts` uses — so moving either fails a test instead of 404ing only inside the image; if the root is configurable the variable is read by the server **and** present in `compose.yml` and `.env.example` (TD-020). **(6) An absent bundle refuses by name**: a process started with no bundle logs which directory is missing, `/readyz` still reads ok and `/api/*` is unaffected (rule 31, rule 18, the shape `startRuntime` already uses for a missing runner); and the roles that serve it are stated and asserted — the ones that serve the API, not `worker` or `launcher`. **(7) `pnpm bundle:check` still runs** and TD-013:8's `'Bundle budget ≤ 300 kB gz initial'` still holds: serving the bundle adds no second build step and no server-side bundling. **(8) The compose-level statement is measured, not assumed** — that `docker compose up` serves the SPA at `/` from the product image and the API at `/api` needs a daemon, so it lands as a check beside `node scripts/runlet-container-check.mjs` rather than as a `verify` target, and a skip counts as a failure (WP-22's precedent). **Needs measurement** (rule 66, not run by the refiner): nobody has yet requested `/` against the built product image |
| WP-16 | Context packs + KB indexer (phase 1 FTS) + code map (ctags + PageRank) + `kb_search` tool | WP-03, WP-12 | retrieval tests on a fixture vault; token budget respected |
| WP-17 | Role prompts + artifact schemas + eval sets (product/13, TD-016): triager, product manager, investigator, architect, developer, reviewer, acceptance tester, facilitator, librarian, discovery; platform skills. **Also owns the three pieces WP-16 built and could not place** (PROGRESS backlog 11): the prompt assembler that turns a `ContextPack` into layers 4–5 of technical/04 § "Prompt assembly" and **delimits untrusted pack text as data** — technical/07's provider-text block says of this obligation *"this is where it closes"*, BD-022 governs, and `context-pack.ts`'s docblock already names WP-17 as the delimiter; `basicStageRunPlanner` passing a real `contextPack` instead of `[]`; and a production `PlatformToolPort` composition root, without which `kb_search` has no home (`platform-mcp.ts` is ready and takes one) | WP-12, WP-16 | promptfoo evals green on fixtures within budget; **a hostile KB document — injection text, a literal `<system>`, `<img onerror=…>`, a `javascript:` URL, an ANSI `ESC[31m` and a U+202E — is rendered inside the pack's data delimiter and cannot open the platform's own voice, asserted against the assembled prompt rather than against the pack**; the delimiter lands **in or before** the change that first passes a non-empty `contextPack`; a stage run writes a `ContextPackRecord` that is not zeroed; `kb_search` is callable from a run |
| WP-18 | Librarian pipeline + proposals + apply policy + knowledge MR flow + nightly hygiene. **Also registers `KnowledgeIndexer` as the pg-boss job technical/07 specifies** — "singleton per project", triggered at task start and after every merge. WP-16 shipped the indexer and its `VaultSource` and deliberately did not wire the job; the reason recorded here was that it needs a checkout to read, which needs the workspace provider — **and that reason was incomplete, which PROGRESS backlog 26 measured and TD-026 now rules on**. Composing the workspace provider at WP-15g hands this row nothing: the only `VaultSource` adapter reads an absolute path on the server process’ own filesystem (`packages/infrastructure/src/knowledge/filesystem-vault.ts:47-48`) and the platform’s checkouts live on Docker volumes the server never mounts. **Architect ruling, recorded as TD-026:** this row builds a **second** `VaultSource` adapter, `createGitVaultSource`, over a **platform-side bare mirror** the platform process clones and fetches itself — `git rev-parse`, `git ls-tree -r -z`, one `git cat-file --batch`, no working tree, no Docker client, no helper container, and **never** the launcher’s `repo-cache` volume (TD-021’s WP-15g amendment forbids the client; TD-026 § Rationale has why the cache volume is refused on freshness). The application-ring port is **unchanged** — no new port, no new method. The mirror root is a new `APP_KNOWLEDGE_MIRROR_ROOT` (TD-020 naming) and its directory name is `mirrorCacheKeyFor(projectId)`, the same function the workspace spec uses at `packages/infrastructure/src/workspace/spec.ts:179`, so the two mirrors cannot be named two ways; the fetch credential is the project’s **existing** git binding credential through WP-15a’s loader and `SecretStore`, passed as a credential-helper environment and never inside a URL. The image and compose half — `git` in the platform image and the data volume — is WP-22’s; this row owns the env var, the refusal when it is absent, and the tests. The code map is **out of scope** and TD-026 § Decision 12 says what it gets instead. **Delivered in two parts** (orchestrator, session 5): **WP-18a** is the `createGitVaultSource` adapter, the mirror, its composition and the `KnowledgeIndexer` job — the seven read-path criteria, the two census guards and the singleton/trigger criterion below; **WP-18b** is the librarian pipeline, proposals, apply policy, knowledge MR flow and nightly hygiene — "proposals appear in UI; thresholds honoured; commits carry provenance" | WP-16, WP-17, WP-15c, and **WP-15a** (the binding loader and `SecretStore` that decrypt the git credential the mirror fetches with). **Not** blocked by WP-15g. The deployment half is WP-22 | proposals appear in UI; thresholds honoured; commits carry provenance; **the index job is singleton per project — two triggers while one run is in flight produce one run — and a merge on a project’s default branch triggers one**. **And seven for the read path, none of which an in-memory vault can pass** — rule 82: the artefact this half produces is the *tree*, so every criterion below is asserted against rows in the database after an index run over a real bare mirror the test built with `git clone --mirror`. **(1) No working tree** — the process’ filesystem holds no checkout of the fixture repository, and `kb_documents` afterwards holds the four indexed path classes at the mirror’s default-branch head. **(2) `repoPaths` is the tree, not a walk** — a path committed to the fixture appears in the snapshot even though no directory on disk contains it, and a file written into the fixture directory without being committed does not appear. **(3) A symlink and a gitlink are listed and never read** — a `.md` entry of mode 120000 pointing at `/etc/passwd` is in `repoPaths`, produces **no** `kb_documents` row, and no byte of that file reaches `kb_chunks`; a mode 160000 path is in `repoPaths` and produces no row. **(4) The mirror is refreshed before the read** — a commit pushed to the seeded remote after the first index run is indexed by the next run with no other trigger and no manual fetch. **(5) A failed refresh is `vault_unavailable` and keeps the index** — with the remote unreachable the report names the failure, and the project’s `kb_documents` count is both **unchanged** and **non-zero**, asserted in both directions because a store that had nothing to keep looks identical from one side. **(6) A commit outside the default branch is refused** — the job pointed at the head of an `agentic/*` branch indexes nothing and reports `vault_unavailable`, which is BD-025 at the read (`git merge-base --is-ancestor`). **(7) Absent configuration refuses by name and writes nothing** — a server started with no `APP_KNOWLEDGE_MIRROR_ROOT` composes no vault source, `/readyz` is still ok, the job’s refusal names the missing variable, and an existing index is not emptied (rule 31, rule 18). Plus two census-shaped guards rather than behavioural ones: `apps/launcher/src/docker-access.test.ts` is left **unchanged** and still passes with the new adapter composed in `apps/server`, and a credential planted in the git binding is absent from both the fetch’s recorded argv and the mirror’s own `config` file after a fetch |
| WP-19 | Cost ledger, rollups, budgets projection, price table maintenance job, estimates. **Also owns the backfill WP-15a's sweep makes necessary**: `run.finished`/`run.failed` are declared `unconsumed` in `EVENT_CONSUMPTION`, so the outbox sweep completes them and a ledger registered later starts empty. Flip both rows to `handled` **and** build a re-dispatch that replays a range of `events` into a newly registered handler (`events` is append-only, and the payload carries `usage`, `model_usage`, `cost`, `num_turns`, `wall_ms`, so nothing is lost — it just has to be read back). See `docs/TODO.md` and TD-005's WP-15a amendment. **It also owns `run_model_usage`'s writer, which no row named before**: nothing in the tree inserts into that table, so `model_usage` on the run DTO is `[]` for every run (`apps/server/src/queries/pipeline-queries.ts:196`, projected by WP-15h), while `run.finished` carries the per-model record in its payload and `normaliseModelUsage` already builds the rows — its docblock names this work package for the 1-hour-cache write it deliberately under-attributes in a multi-model run (`packages/infrastructure/src/runner/transcript-normaliser.ts:185-193`) | WP-04 | reconciliation test: sum of entries = rollups; budget pause behaviour; **a run that finished before the ledger's handler was registered appears in the rollups after a backfill**; **a finished multi-model run's per-model usage is readable through `GET /api/runs/:run_id` rather than `[]`**, from rows the ledger wrote rather than a seeded table |
| WP-20 | Web app foundation (TD-013): auth screens, layout, theme, realtime client, Query bridge, board, task detail, run detail with transcript renderer, agents view, inbox, settings, integrations setup, pipeline settings, budgets, statistics, audit log, knowledge browser + proposals | WP-06 | Playwright e2e with fake SSE; bundle budget check |
| WP-21 | Onboarding wizard steps 1–5 incl. discovery agent and readiness evaluation (product/06, /17) | WP-16, WP-17, WP-20 | wizard completes on fixture repo; readiness report produced |
| WP-22 | Docker images (base, runtime, egress, launcher, product), Compose (local mode as a `compose.local.yml` override — TD-018's amendment says why a profile could not express it), `image.yml`, `base-image.yml`, size checks, attestations. **Also owes the three measured things PROGRESS backlog 7 carries** (the `User nobody` / uid-1000 mismatch in `renderEgressConfig`, `apps/runlet` bundled to one file with `scripts/runlet-container-check.mjs` re-run against the real image, and the missing env companion for `WorkspaceImages.egressCommand`), and **is the package that lets the real provider be tested against the real images** (PROGRESS backlog **27**) | WP-14 | `docker compose up` on a clean VM yields a working instance; images signed; **and the real images replace the stand-ins in the tier that already runs the real provider** — `test/e2e/workspace/docker-workspace.e2e.test.ts` runs against `platform-runtime` and `platform-egress` with `runtimeSourceDir` **unset** (no `/repo` bind mount, which technical/05 forbids in a run container, and therefore with the shim started from the image's own entrypoint) and with `egressCommand` empty, and it is CI's `e2e-fake-claude` job that runs it — a green `node scripts/runlet-launcher-check.mjs` or `runlet-container-check.mjs` does **not** satisfy this criterion, and neither does a case that skips when the image is absent: **a skip counts as a failure here**, so `image.yml` publishing a tag the e2e job pulls is part of the criterion rather than a follow-up. **Amended after WP-22 measured it** (refiner, session 5): the criterion is met by a **stronger** route than the one it names, and the row says so rather than leaving a sentence the change does not literally satisfy. CI's `e2e-fake-claude` job **builds** `platform-runtime` and `platform-egress` from the commit under test instead of pulling a published tag — it therefore tests *this* commit's images rather than the last tag pushed, and needs no registry round trip — and the no-skip half is enforced where it belongs rather than by a registry being reachable: `ensureImages` **throws**, naming `node scripts/build-images.mjs runtime egress`, when a `platform-*` image is absent, so a missing image is a failure and can never become a skip. `image.yml` still publishes the tags; nothing in CI's e2e path depends on that publication, which is what removes the ordering between the two workflows. Within that tier, **the readiness handshake is asserted against the real provider by a case in which the shim begins listening after `attach` was called** — calibrated in both directions the way the unit half was (shortening `#waitForControlSocket` (`packages/infrastructure/src/workspace/provider.ts:785`) to a single look fails that case **by name**; unmutated it passes) — because `create` returning and the socket listening are different instants and the real image changes the gap between them, which is the one thing the stand-in cannot measure. And **the rendered egress config starts the real sidecar**: asserted by a proxied request served through `platform-egress` running as uid 1000 with `cap_drop ALL`, not by the contents of the file `renderEgressConfig` wrote |
| WP-23 | Docs: operator guide (install, integrations setup, upgrade, backup), user guide, `THIRD_PARTY_NOTICES.md` | WP-22, **and after WP-15i and WP-15j** — the user guide describes task and run commands that must exist and an instance whose `/` must answer the SPA; written earlier it documents two 404s | a stranger installs in < 1 hour (dogfood test) |

## Milestone M2 — trust
WP-24 review-only mode; WP-25 ticket readiness linter; WP-26 rebase gate + conflict warnings; WP-27 steer + take-over/hand-back (export, resume instructions); WP-33 nightly real-LLM smoke + evals in CI (`llm-ci` environment). **WP-28…WP-32 have rows of their own in the table below** (written by a refiner in session 5, at `785941f`): they were one-line items with no acceptance criteria while M1's rows carried them, and each one turned out to have a shipped half, a half nobody owned, or both — the estimate exists and its gate does not, the autonomy dial is a word in a column nothing reads, and the whole Slack notify band technical/02 attributes to the finished WP-10 has no open owner at all.

**WP-27 additionally owes the 14-day retention window** for a paused or taken-over workspace. `buildWorkspaceSpec` writes three days at create time because nothing then knows how the task will end (`packages/infrastructure/src/workspace/spec.ts:169,187`) and no path ever relabels the volume, so technical/05:10's *"14 days for paused/taken-over"* is unimplemented: a taken-over workspace is purged on day 4 with the human's work in it. The evidence is PROGRESS backlog **7**'s retention bullet; acceptance is that a workspace whose task was taken over survives the sweep past day 3 and is purged after day 14.

*Refiner (session 5): **met by WP-27's `extendRetention`**, pending that row's merge — and the sentence above is wrong about why it had not been done. A relabel was never available: measured against Docker Engine 29.7.2 / API 1.55, `docker volume create` with an existing name and a different label **succeeds and keeps the original labels**, and `docker volume update` answers "can only update cluster volumes". The implementation therefore writes a second, marker volume `hold-<run-id>` and the sweep takes the later of the two instants; the criterion is unchanged and is asserted through the shared contract suite against both the fake and a real daemon (held past day 3, removed after day 14, never shortened). **technical/05 owes a sentence naming `hold-<run-id>`** — the measurement and the amendment are in PROGRESS backlog **7**'s retention bullet. Four things WP-27 did **not** carry are backlog **67–71**: the misrouted return-reason channel (67, live defect), the export's download surface (68), `paused_reason` plus product/19 § 19's unbuilt 5-working-day timeout (69), the take-over/hand-back screen (70, cheapest by adjacency on **WP-30** if it is wanted in M2) and the run workspace's missing checkout branch (71, a criterion for whoever composes a production provisioner — **Q52**).*

| WP | Title | Depends on | Acceptance criteria |
|---|---|---|---|
| WP-28 | **The estimate is shipped and its gate is not — `requiresBudgetApproval` is defined, unconsumed, and reads a threshold no query can answer.** WP-19 already built the number half of this row: `costEstimateHandler` runs on `artifact.created` at TD-005 priority 15 and writes `tasks.size` and `tasks.estimate_usd` through the narrow `CostStore.saveEstimate` (`packages/application/src/cost/estimate.ts`, `packages/infrastructure/src/cost/postgres-cost-store.ts:417`), over the model in `packages/domain/src/cost/estimate.ts` (`estimateTaskCostUsd`, `estimateAccuracy`, per-size weights `S:1 M:2 L:4 XL:8`). So what this row owns is **the gate**, and the gate's function exists with no caller: `requiresBudgetApproval(preset, estimateUsd)` at `packages/domain/src/policies/autonomy.ts:219-220`, and a grep over `packages/` and `apps/` finds the definition and nothing else. PROGRESS's WP-21 note 4 states it in those words — *"`requiresBudgetApproval` is still unconsumed, and that is WP-28's… This row sets the dial; it does not build the gate the dial configures"* — and Q65's closing sentence says the same. **The threshold it would read is not stored**: `PUT /api/projects/:project_id/config` calls `applyAutonomyPreset(level)` and puts exactly two of the preset's sixteen fields into a `human_actions` params blob (`apps/server/src/routes/onboarding.ts:568-580`, `probation_tasks` and `plan_approval`), while `projects` stores only the **level** — so nothing can answer "what is this project's `budgetApprovalThresholdUsd`", which is why WP-30 comes first. **The mechanism is a second `kind` on an existing path, not a new one**: `approval_kind` already contains `'budget'` (`packages/infrastructure/src/db/migrations/0002_enums.sql:94`), `planApprovalGate` is a working precedent keyed on `(task, kind, stage, attempt)` (`packages/application/src/pipeline/saga.ts:551-618`), and `decideApproval` plus `POST /api/tasks/:task_id/approvals/:approval_id/decide` shipped at WP-15i. **The estimate does not reach the workpad** although product/18:31 says it is *"shown in the workpad"*: `renderWorkpad` prints `Cost so far: <x> of <y> USD` and nothing else about money (`packages/application/src/pipeline/workpad.ts:132`). **And the number's own basis is thrown away**: `saveEstimate` writes two columns while `basis` (`project_history`, `org_history`, `unknown`) and `samples` are only logged (`packages/application/src/cost/estimate.ts:96-106`), so an approval card cannot say whether the figure rests on this project, the organisation, or nothing. A *budget row* (BD-010's org and project caps) also has no production writer — `insert into budgets` occurs twice in the tree and both are test files — but that is **WP-30's** step-4 item 4, named on its row; this gate reads the dial's threshold and needs no `budgets` row. **What it costs to leave**: product/19:111 publishes a *"Budget approval threshold"* of $20 at Assist and $50 at Supervised, and the UI will print it as a policy while nothing enforces it; product/09:27's *"Estimate before spend"* stops at "estimate", and the only thing that actually stops spend before a stage is the per-task cap the executor checks against project configuration | WP-19, WP-15i (both landed); **WP-30 first** — it is the row that stores the materialised threshold this gate reads, and a gate that re-derives it from the level at decision time violates BD-027:14. Backlog **50** (a cancelled run's spend reaches no `cost_entries` row, so both the accuracy metric and any budget window understate — Q70 (b)). Q65 (the estimate model, implemented). **Blocked on human: Q71** — which number crosses the threshold and what happens when there is none — filed with a recommendation strong enough to build from without an answer. Node and Postgres only; no daemon | **(1) The gate is a `kind: 'budget'` approval on the path that already exists.** Created in the saga beside `planApprovalGate`, keyed through `approvals.forStageAttempt` so a re-refinement does not ask twice — *(**the mechanism half of this clause was falsified at implementation and is superseded; the property is the one shipped**, refiner note, session 5: the two clauses cannot both hold, because `enteredAttempt` (`packages/domain/src/aggregates/task.ts`) increments on **every** re-entry, so an attempt-keyed lookup asks again on attempt 2. Canaried: swapping the shipped lookup back to `forStageAttempt` fails `saga.test.ts` › *"does not ask again when the task goes round refinement a second time"* by name and nothing else. The gate keys `(task, kind)` through a new port method `ApprovalRepository.latestOfKind`, added to the shared contract suite in the same change (rule 23) — a plan approval is a statement about *a plan*, which is re-made, and a budget approval is a statement about *the estimate*, which is written once. `planApprovalGate` keeps `forStageAttempt` and is **correct** to: checked by the refiner against the same re-entry mechanism, a second plan is a second thing to approve, and the contract suite pins both questions)*, decided through WP-15i's existing decide route, with **no second route and no new task state**. Asserted from **both** sides (rule 42): a task whose estimate is above the threshold waits, and one at or below it is not gated — a gate that fired on everything would pass the first half alone. **(2) No estimate means no gate, and the task says so.** `estimateTaskCostUsd` answers `usd: null, basis: 'unknown'` for a project with no finished task, and that must not be read as zero (rule 16) or as *gate everything*; asserted with an empty-history project, and the absence is named on the workpad and the task DTO rather than rendered as a blank. **(3) The threshold is read from stored, materialised policy.** The change names the column or declared configuration key WP-30 writes and reads *that*; a test proves BD-027:14 by editing `AUTONOMY_PRESETS` in the source and showing an existing project's gate does not move. **(4) The estimate reaches the workpad, with its basis.** A forward-only migration (TD-011, next number after 0019) adds the basis and sample count beside `estimate_usd`; `renderWorkpad` prints the estimate beside `Cost so far` and stays a pure function of the task row; the edit goes out through the existing `pipeline.outbound` workpad duty, never from a handler, and `events/open-transaction.ts` keeps refusing the alternative. **(5) Approved and rejected both have an ending, asserted as countable effects and not as status codes** (rule 79): approval resumes the stage the gate suspended, rejection moves the task to a state the aggregate decides, each leaves exactly **one** `approvals` row, one event and one `human_actions` row, and a replayed decision leaves none extra. An expired budget approval follows the plan gate's own timeout behaviour rather than inventing a second one. **(6) The estimate is never re-made and the accuracy is published.** The write-once guard (`estimateUsd !== null`) is asserted across two refinement rounds; `estimateAccuracy` appears on the task DTO, and product/19:99's *"Cost estimate accuracy"* is computed from `tasks.estimate_usd` and `tasks.cost_actual` and from nothing else. **(7) Driven in the e2e tier against a real `apps/server` instance, with a fake runner that picks its scenario from the spec rather than from `spec.stage`** (rule 82) — the case needs a run whose recorded cost differs from the estimate, which a stage-dispatching fake cannot express. **(8) Rule 83: the sentences this falsifies are found by grep and corrected** — PROGRESS's WP-21 note 4, Q65's last sentence, the docblock at `packages/domain/src/policies/autonomy.ts:219`, and `EVENT_CONSUMPTION` if a new type appears |
| WP-29 | **`human_time_entries` has existed since migration 0007 and has never held a row — the projector technical/03 names does not exist, and one of the four kinds product/19 defines cannot be stored at all.** The table is `packages/infrastructure/src/db/migrations/0007_cost.sql:110-122` with indexes on `(task_id)` and `(user_id, started_at desc)`, typed in Drizzle at `packages/infrastructure/src/db/schema/cost.ts:126`, and technical/03:88 describes it as *"derived from events by a projector"*. There is no projector: a grep for `human_time` over `packages/` and `apps/` returns the migration, the Drizzle table and the enum, and no reader or writer. **The enum cannot express one of the four kinds**: `human_time_kind` is `('review', 'question', 'approval')` (`0002_enums.sql:122`) while product/19:145 defines *"steer = 5 min flat per steer"*; `run.steered` is declared `'unconsumed', // WP-27 steer` (`packages/application/src/events/consumption.ts:104`), so the fourth kind has neither a producer nor a value. **Two of the three review anchors have no event.** product/19:145 starts the review window at *"the first human MR activity (comment, approval, review start)"*; technical/02:130-131 gives the catalogue `mr.opened`, `mr.updated`, `mr.merged`, `mr.closed` and `mr.review.comment`, and **no `mr.approved` and no review-requested type** — `mr.review.comment` and `mr.merged` are `handled`, `mr.updated` is `'unconsumed', // WP-41 statistics`. **Per-user attribution has nothing to attribute to**: `human_time_entries.user_id references users(id)`, and `user_identities` — the only mapping from a provider's author onto a platform user (`0003_identity.sql:39`) — is **read** in exactly one place (`packages/infrastructure/src/integrations/postgres-inbox.ts:160`) and **written nowhere** in `packages/` or `apps/` — **a writer landed at WP-31** (`POST /api/org/identities`, admin; PROGRESS backlog **79**), so the null is now *unmapped* rather than *unmappable*: the table is empty until an operator maps an account and no screen calls either route, which is what this row must still assert against. So on this build every minute derived from an MR comment has `user_id: null`. **What it costs to leave**: product/09:29 and product/18:32 make *"total cost of delivery = tokens + people"* the platform's ROI argument and product/16:29 makes it a tracked metric; the dogfood plan's Phase B (product/19:160) records cost per merged task and can record only the token half. The statistics dashboard is **not** this row's — `GET /api/org/stats` has no route, no DTO and no rollup, and `apps/web/src/features/statistics.tsx:35` says so on the screen; that is WP-41's in M3. This row owes the **entries** | WP-19 (landed — the cost half the minutes sit beside, and `events/replay.ts`, the backfill that lets a handler registered today be served events swept last month). WP-27 for the `steer` kind and for a `run.steered` consumer; the other three kinds do not wait on it. WP-41 consumes the entries in M3 and does not block. **Blocked on human: Q73** — no document gives an hourly rate, so the single summed number cannot be computed honestly; the recommendation is to ship minutes and USD unsummed and gate the sum on an optional org setting. Node and Postgres only | **(1) One projector, one writer, idempotent under redelivery.** Handlers at declared TD-005 priorities write `human_time_entries` and nothing else does (a census in the shape of `tasks-column-ownership.test.ts` if a second writer is ever plausible); dispatching the same event twice produces the **same** rows, asserted by counting rows rather than by a return value (rule 79). **(2) Every cap and exclusion of product/19:145 is its own criterion, asserted at the boundary and one unit inside it** (rule 42): the 8 h per calendar day cap, *"excluding gaps > 2 h"*, 30 min per question, 10 min flat per approval, 5 min flat per steer — for each, a case at the value and a case past it, since a projector that always clamped would pass a one-sided test. **(3) The kinds the enum cannot express are decided in the tree, not dropped.** Either a forward-only migration adds `'steer'` to `human_time_kind` (TD-011: a new numbered file, never an edit to 0002) or the change states why steer minutes are not counted and product/19:145 is amended; and the review window names the event that supplies each of *"comment, approval, review start"* or states plainly that it starts at the first `mr.review.comment`, with the residual — a reviewer who approved without commenting contributes zero minutes — written at the code rather than implied. **(4) `EVENT_CONSUMPTION` is updated for every type consumed and the sweep-readiness gate stays satisfied.** If the projector needs `mr.updated`, that entry's `// WP-41 statistics` comment is now wrong and is corrected in the same change (rule 83); a type that stays unconsumed keeps naming the work package that will flip it. **(5) An unmapped identity stores `user_id: null` and the totals are still right.** Asserted with an event whose author matches no `user_identities` row — which is *every* event on an instance where nobody has mapped an account — the table has had a writer since **WP-31** and is empty by default, so both directions are now assertable: a mapped author attributes, an unmapped one stores `user_id: null` — and product/19:145's *"totals per task always shown"* holds in the presence of nulls; the per-user breakdown defaults **off** (product/18:32) and the default is asserted as well as the override. **(6) Minutes and dollars are two fields and are never added by this row** (Q73). The DTO carries both; no code path multiplies minutes by a rate this build does not have. **(7) The backfill works.** The projector is registered through `events/replay.ts` and a replayed range of `events` produces rows **equal** to those a live dispatch produced — an equality, not a spot check — and a second pass is a no-op, exactly as the cost ledger's backfill is. **(8) Driven from real events in the integration or e2e tier, never from a seeded `human_time_entries` table** (rule 82): the fake must produce the MR and question events the projector folds, or the tier certifies nothing about the fold |
| WP-30 | **The autonomy dial is a word in a column that nothing reads, and BD-027's one consequence — materialise at selection time — is the half that was not built.** `PUT /api/projects/:project_id/config` accepts `autonomy_level`, refuses a document whose `policies.autonomy` disagrees with it, writes `projects.autonomy_level`, and calls `applyAutonomyPreset(level)` — whose result reaches **only** the audit row's params, two of sixteen fields (`apps/server/src/routes/onboarding.ts:500-583`, `probation_tasks` and `plan_approval` at `:568-580`). BD-027:14 requires the opposite: *"Preset tables are versioned; changing a preset definition in a release never silently changes a project's effective policies (they are materialised at selection time and the UI offers 're-apply preset')."* Because only the word is stored, editing `AUTONOMY_PRESETS` in a release changes every project's policies at once. **Nothing in the pipeline reads the dial.** The one gate that could is `planApprovalGate` (`packages/application/src/pipeline/saga.ts:551-618`) and it reads `config.pipeline.template_overrides[…].plan_approval` and `size_threshold` — not `AutonomyPreset.planApproval`, not `probation`, not `planApprovalForRiskClasses`; `requiresPlanApproval` (`packages/domain/src/policies/autonomy.ts:195`) is defined and unconsumed exactly as `requiresBudgetApproval` is. So `picksUpNewTickets`, `stopAfterStage`, `businessReview`, `humanMrRounds`, `knowledgeAutoApply`, `reviewOnly` and `shadowMode` each have **no reader**, and product/19:111's preset table is a document rather than a behaviour. **Step 4 is one of five sub-steps.** `apps/web/src/features/onboarding.tsx:327-393` is four radio buttons and the `PUT`; product/18:50-54 asks for five things in that one step — dial, feature toggles with product/19:124's card fields, risk classes proposed from the repository structure, budgets including a separate shadow and maintenance budget, and notifications (channel, quiet hours, digest). **There is no project settings page for the mirror to live in**: `apps/web/src/features/settings.tsx:1-9` is *org* settings and its own docblock says autonomy defaults, provider mode, global budgets and feature flags *"need `GET/PATCH /api/org`, which no work package has built"*, while product/10:21 specifies the project settings page as *"autonomy dial, features (mirror of the wizard), WIP limits, policies, risk classes, budgets, Slack channel, quiet hours"*. **Budgets have no production writer at all**: `insert into budgets` appears exactly twice in the tree and both are test files (`test/integration/cost/postgres-cost-store.integration.test.ts:126`, `test/e2e/support/pipeline.ts:894`); the read is served (`apps/server/src/routes/projects.ts:163`) and technical/08's own unbuilt list names `GET/PUT /api/projects/:id/budgets` — so BD-010's org and project caps are inert on a real instance and WP-19's admission guard reads an empty table. **Which mirror this row owns was explicitly left to its planning and is decided here**: PROGRESS's WP-21 bullet says *"Say which mirror when WP-30 is planned: BD-028's wording covers step 4's settings, product/18:55 covers all five steps"* — product/18:55 is the broader and unconditional statement (*"Settings pages mirror the wizard one-to-one, so nothing is only reachable during onboarding"*), so this row owns **all five**, which makes the integrations create and test buttons this row's too, as that bullet already attributed them (both endpoints exist since WP-21; `apps/web/src/features/integrations.tsx` says at the top that they are driven from the wizard). **What it costs to leave**: the product's single explainable control does nothing — a maintainer who moves a project to Autonomous gets the behaviour of Observe — and the audit row records that the dial *meant* something it did not; every choice made in the wizard is reachable only during onboarding, which product/18:55 forbids | WP-21, WP-20, WP-15i (all landed). Backlog **52** (`human_actions` has eighteen writers and no reader but the idempotency guard, while product/18:5 requires *"Every toggle records who changed it (audit)"* to be **visible**) and backlog **53** (the SPA mints a fresh `Idempotency-Key` per request, so a double-submitted settings form is two writes). Q69 bounds it: a project may only *narrow* the organisation command maximum, so no dial level can widen what a run may execute. **Land before WP-28**, which reads the threshold this row stores, and before WP-32, whose channel, quiet hours and digest are step 4's item 5. **Blocked on human: none** — product/19:111 and product/19:124 give every default. Node and Postgres only | **(1) The materialised preset is stored, versioned and read back.** Selecting a level writes every `AutonomyPreset` field plus `AUTONOMY_PRESET_VERSION` where a query can read it (a column or a declared key of `projects.config`), and a test proves BD-027:14 directly: change `AUTONOMY_PRESETS` in the source, re-read an existing project, get the **old** policies; invoke "re-apply preset", get the new ones. **(2) *Custom* is computed from the stored preset, not from the level**, through the existing `describePresetOverrides`, published by the API and listed in the UI (product/18:9) — asserted both ways (rule 42): a project with no override is **not** shown as Custom. **(3) At least one policy the dial sets is enforced by the pipeline, and every one that is not is listed.** `planApprovalGate` reads the stored preset through `requiresPlanApproval` — including probation and risk classes — instead of only `template_overrides`; each remaining field with no reader is enumerated in the tree beside the work package that will read it, the way `EVENT_CONSUMPTION` enumerates an unconsumed event (rule 18: the absent case must not be the quiet one). A change that stores sixteen fields and enforces none fails this criterion. **(4) Step 4 carries all five of product/18:50-54, or names each missing one as a gap on the screen** — the shape step 3 already uses at `onboarding.tsx:320-325`, not controls that silently do nothing. Feature toggles carry product/19:124's card fields: name, one-sentence value, default, cost implication, what it touches externally. **Review-only mode is the first of those cards with a shipped behaviour behind it and no control anywhere** (WP-24, session 5): the mode is configured only in `.agentic/config.yml` or `projects.config`, and what turns it on is BD-028's opt-in key — `features.review_only.enabled` plus `trigger` (`label | all | paths`), `label`, `paths`, `severity_floor`, `max_findings` — **not** `AutonomyPreset.reviewOnly`, which this row's criterion 3 lists among the fields with no reader. The card writes the opt-in key; if it also drives the preset field, say which of the two wins, because today they are two switches for one feature and only one of them is wired. Two reads this card depends on are defects this row will meet first: backlog **58** (a stored `trigger: manual` makes `GET /api/projects/:id/config` — the read step 4 opens with — answer `500 invalid_stored_config`, naming no key) and backlog **55**. **(5) `PUT /api/projects/:project_id/budgets` exists and is the row WP-19's guard reads.** It takes `Idempotency-Key` performing nothing twice, writes one `human_actions` row per accepted write and none for a refused one, answers 401 per route and at least one wrong-role 403, and an **end-to-end** assertion shows a budget created over HTTP pausing a task through `cost/guard.ts` — because today `insert into budgets` exists only in two test files, an assertion against a seeded row would certify nothing (rule 82). **(6) The mirror is checked, not claimed.** A census in the shape of `apps/server/src/routes/client-census.test.ts` compares the wizard's command set against the settings screens', fails in **both** directions so the list cannot go stale, and names any deliberate omission in the test itself; the integrations create and test buttons are part of the comparison. **(7) Every toggle is audited and the audit is readable** (product/18:5, BD-003): each write records actor, before and after, and the change is visible through an audit read rather than only in a row nothing queries (backlog 52). **(8) Readiness only ever suggests.** `suggestedAutonomyCap` caps the suggestion and never the choice; a maintainer's visible override is accepted (product/18:18, BD-026, Q21), asserted with a readiness-level-0 project set to Autonomous. **(9) Rule 83: grep for the sentences this falsifies** — `settings.tsx:1-9`'s docblock, step 4's text in `onboarding.tsx`, PROGRESS's WP-21 bullets about the integrations buttons and about which mirror this row owns, and the "unconsumed" notes in `packages/domain/src/policies/autonomy.ts` |
| WP-31 | **Ask-the-task has an endpoint nobody built, an inbound event nobody consumes, and no vocabulary anywhere for what an "ask" is.** technical/08:17 lists `POST /api/tasks/:id/ask` and technical/08:100-103 puts it on the unbuilt list — *"`POST /api/tasks/:id/{take-over,hand-back,ask}` (WP-27 and WP-31)"* — and WP-15i's row adds the part that matters for verification: the SPA does not call it, so `apps/server/src/routes/client-census.test.ts`, which compares the **client's** paths against the router, is blind to its absence by construction. **The ticket-side half has no consumer**: `ticket.comment.added` is declared `'unconsumed', // WP-31 ask-the-task; feedback intake is WP-24's` (`packages/application/src/events/consumption.ts:99`), so a question typed into the ticket thread — which product/10:57 requires to be mirrored there — reaches the `inbox` row WP-15c writes and stops. **Nothing in the platform's vocabularies can name an ask**: `run_mode` is `('normal','shadow','review_only','linter','discovery','retro','librarian')` (`0002_enums.sql:46-54`), `agent_role` has ten values and none is an ask role (`:34-45`), `packages/prompts/roles/` has ten directories and none is one, product/13's *"Tools per role"* table has no row for it, and the `questions` table is the **opposite** direction — technical/02:24 defines a Question as *"a stage's request for human input"*, so a human's question has no storage. **The tool an answer would be built from refuses by name**: product/10:57 says an ask is *"answered from the audit trail and artifacts with links to the exact run and prompt"*, and `get_task_context` is one of the eight platform tools that refuse (`apps/server/src/platform-tools.ts:118`); `kb_search` is the only implemented one (`:89`). **The precedent is WP-21's and it is the easier case**: Discovery had to become a stage of a one-off task because `runs.task_id` is `not null`, whereas an ask already **has** a task and `runs.task_stage_id` is nullable exactly for this — technical/03:40-42 names ask-the-task as one of the three run kinds with no stage — so the admission guard, the cost ledger, the transcript sink, the budget cap and the escalation all apply unchanged. **What it costs to leave**: product/18:34's value is *"Trust through explanation; cheaper than reading transcripts"*, and the only explanation surface this build has is the raw transcript on the run page. It is also the row that gives `ticket.comment.added` its first consumer — the door every ticket-side conversation arrives through | WP-15i, WP-15h, WP-17, WP-19, WP-15c (all landed: the command shape and `human_actions`; the read endpoints an answer's citations resolve through; the prompt assembler and data-block contract; the ledger and admission guard an ask run is charged through; the inbound door). **Shares `ticket.comment.added` with WP-24** — whichever lands first registers the dispatch and the other adds a consumer beside it, and `EVENT_CONSUMPTION`'s comment names both today. **Blocked on human: Q72** — which role, whether it is a run, where the thread lives, whether the ticket mirror is on by default, and who may ask; filed with a four-part recommendation that can be implemented without an answer. Node and Postgres only; no workspace and no daemon | **(1) An ask is a run with a task and no stage.** `runs.task_id` set, `runs.task_stage_id` null, a declared `run_mode` value added by a forward-only migration, and it passes through the **same** admission guard, cost ledger, transcript sink and budget cap as every other run — no second entry point (`packages/application/src/onboarding/discovery.ts` carries the argument). Asserted by an ask refused because the project's budget is exhausted, read back from `cost_entries` rather than from a status code. **(2) The question is untrusted in both directions.** It reaches the model only inside an `assemblePrompt` data block (`packages/domain/src/prompt/`) with the per-prompt nonce, never concatenated; the **answer** is rendered by the SPA as React text nodes through `apps/web/src/ui/untrusted.tsx` with every URL through `safeHref`, which `apps/web/src/no-html.test.ts` already enforces (BD-022). **(3) Nothing it stores carries a secret.** Question, answer, transcript and any ticket mirror go through the run-scoped TD-012 redactor the way `tasks.ticket_snapshot` does (`packages/application/src/pipeline/ticket-snapshot.ts`), and the byte bound on each is **derived at the cap** rather than chosen — this is the fourth place the platform stores untrusted external text after `inbox`, `kb_chunks` and `ticket_snapshot`. **(4) Any ticket comment is an outbound duty.** It goes through `IntegrationActionExecutor` from a `pipeline.outbound` job (`packages/application/src/pipeline/outbound.ts`), never from inside a handler's transaction; `events/open-transaction.ts` refuses the alternative mechanically on both paths. **(5) `ticket.comment.added` is classified in both directions** (rule 42): a comment that is an ask produces exactly one ask, and a comment that is not — a plain remark, the platform's own workpad edit, a feedback phrase — produces **none**; a comment from an unverified identity is refused (technical/02:161, BD-022), which on this build is every ticket-side comment, because `user_identities` has no writer, and the change says so rather than implying otherwise. **(6) The answer cites what it rests on and cannot cross a project.** Each answer carries run ids and artifact versions that resolve through WP-15h's existing read endpoints, and a citation naming another task's or another project's run is refused (product/11:30). **(7) The route matches WP-15i's command shape and is named positively in a test of its own**, because the client-driven census cannot see a route the SPA does not call: strict snake_case body, `Idempotency-Key` performing nothing twice asserted on a countable effect (one run, one `human_actions` row), one `human_actions` row per accepted ask and none for a refused one, 401 per route and at least one wrong-role 403. **(8) Driven end to end with a fake runner that picks its scenario from the prompt, not from `spec.stage`** (rule 82) — an ask **has** no stage, so a stage-dispatching fake cannot express this case at all, which is precisely the instrument failure that rule records. **(9) An ask's cost is an ordinary ledger entry**, visible on the task's cost breakdown and distinguishable from stage spend; the per-question cap (product/18:34) refuses by name rather than truncating, asserted at the cap and one unit under it. **(10) The task-scoped audit read exists, because criterion 6 cannot be met without it** (added by a refiner after WP-30, session 5; PROGRESS backlog **52**'s remaining half). product/10:57 defines an ask as *"answered from the audit trail and artifacts with links to the exact run and prompt"*, and on this build **no read surface serves a task's `human_actions` rows**: WP-30 shipped `GET /api/projects/:id/audit` on the predicate `params->>'project_id'` (`apps/server/src/routes/settings.ts`), which a task command's row — WP-15i's eleven and WP-27's three — never matches, because `human_actions` has no `project_id` column. The index is already `(task_id, created_at desc)` (`packages/infrastructure/src/db/migrations/0004_pipeline.sql:219`), so it is one query and a DTO; `params` is client-supplied JSON carrying a client-chosen `Idempotency-Key`, so it renders through the untrusted path like everything else (BD-022), and technical/08 gains the published path first (an orchestrator edit — a refiner writes no technical document). Whether the ask reads it through `get_task_context` — one of the eight platform tools that refuse by name (`apps/server/src/platform-tools.ts:118`) — or through the HTTP endpoint is this row's decision; both need the same projection |
| WP-32 | **This build sends no notification of any kind, so "digest + quiet hours" is being asked to batch messages that do not exist — and the whole Slack notify band technical/02 attributes to WP-10 has no open owner, because WP-10 is DONE.** `CommunicationPort` is fully specified — `postTaskThread`, `postQuestion`, `postApproval`, `postMessage`, `updateMessage`, `postDigest`, `resolveIdentity` (`packages/application/src/ports/integrations/communication.ts:102-144`) — and **nothing calls any of them**: a grep for `communication` across `packages/application/src`, `packages/integrations/src/bindings` and `apps/server/src` returns exactly one line, the re-export at `packages/application/src/index.ts:93`. **The binding is never resolved**: `createPipelineIntegrationsLoader` builds git and task_management only (`packages/integrations/src/bindings/loader.ts:268-299`), and the shipped registry registers two of five providers with the reason written down — *"Registering Slack, Sentry and Loki here as well would be five entries of which three are constructed by nothing… They are registered by the composition root that consumes them (the digest job, the bug-task pre-fetch), in the work package that builds it"* (`packages/integrations/src/bindings/shipped-registry.ts:8-13`). **That work package is this one, and the ledger currently points at a finished row**: `consumption.ts:112-114` attributes `budget.threshold.reached` and `budget.exhausted` to *"Slack (210), WP-10"*, and WP-10 is DONE at `fbf0928`. technical/02:108-142 puts a Slack consumer at priority 210 on `task.created`, `task.stage.returned`, `task.question.asked`, `task.escalated`, `task.cancelled` and `task.completed` as well. **The mechanism half is built and says so**: `packages/integrations/src/providers/slack/digest.ts:1-26` is a `digest.slack` queue, a cron schedule carrying its own timezone (Q38), an injected clock, one outbound call through `IntegrationActionExecutor` keyed `slack:digest:<channel>:<day>`, and an empty digest deliberately not posted — and its docblock reads *"WP-32 owns the digest and quiet hours: what goes in a digest, whether it is sent at all, and what happens inside an organisation's quiet window are product decisions with their own work package. This module is what that decision will run on."* Nothing schedules that queue in any composition root. **The configuration half exists and is short one field**: `features.digest.{enabled, at, quiet_hours{from,to}}` is in the strict config schema (`packages/contracts/src/config.ts:190-199`), and product/18:33's third configurable — *"urgent classes (escalation, budget 100%) still immediate"* — has nowhere to be stored, so urgency would be hard-coded. **And `resolveIdentity`'s destination has no writer**: `user_identities` is read at `packages/infrastructure/src/integrations/postgres-inbox.ts:160` and inserted nowhere, so a Slack reply cannot be attributed to a platform user. **What it costs to leave**: product/03:17-25's journey is a person watching a ticket and a Slack thread. On this build the only surface is the SPA, which WP-15j is still making reachable at all; every `task.question.asked` and every `task.escalated` waits for someone to open a browser, and BD-006's one-working-day question timeout expires against a human nobody told | WP-10, WP-15d, WP-15a (all landed: the provider and its digest mechanism, the `pipeline.outbound` duty shape every notification must use, the binding loader this row extends to a third type). **WP-30** for the wizard's step 4 item 5 and the settings mirror — channel, quiet hours, digest. WP-27 and WP-31 each add a notification class and neither blocks. Q42 (TD-024 names `@slack/bolt` and `@slack/web-api`; WP-10 shipped without them) is already answered in the tree and does not block. **Blocked on human: none** — product/18:33 and product/19 give the defaults: digest on at 09:00 org time, quiet hours off, urgent = escalation and budget 100 %. Node and Postgres only; no daemon | **(1) A communication binding resolves, both ways.** The loader answers a `communication` port for a project that has one and `null` for a project that does not — the shape `git: null` already has (`loader.ts:15-29`) — while a binding that **fails to load** throws rather than degrading into silence (rule 20's asymmetry is about an inbound delivery, not about a misconfigured credential on an outbound path). Slack joins the shipped registry and `shipped-registry.ts:8-13`'s sentence is corrected in the same change (rule 83). **(2) Every notification is sent from a `pipeline.outbound` duty and never from a handler**: a handler decides, the job calls, and `events/open-transaction.ts` refuses the alternative on both the handler path and the job path, so no provider call happens with a transaction open. **(3) The notify band exists and is declared.** Each type consumed flips in `EVENT_CONSUMPTION` with its handler at TD-005 priority 210, `sweepReadiness` stays satisfiable for a process whose `ROLE` sweeps (`all` or `worker`), and every type still `unconsumed` names the work package that will flip it — including the correction of the two budget entries, which name a finished work package today. **(4) Quiet hours defer and never drop, asserted from both sides** (rule 42): a non-urgent notification raised inside the window is delivered in the next digest and is not lost, and one raised a minute outside it is delivered immediately — both with an injected clock, never a wall clock (rule 2), and both boundaries asserted at the instant and one minute either side. **(5) Urgent classes are configuration, not a constant**: `features.digest` gains the field product/18:33 names, defaulting to escalation and budget-100 %, with the default asserted **and** an override asserted. **(6) The digest is idempotent on a countable effect.** The job run twice for one day posts **one** message through the existing `slack:digest:<channel>:<day>` key and an empty day posts **none**, both read back from `integration_actions` rather than from the port's return value (rule 79). **(7) A shadow-mode project posts nothing and records `would_have`.** Every notification and the digest carry the task's `mode` so the executor's shadow guard can refuse (`digest.ts:20-22`), asserted with a shadow task; a shadow project whose digest posted would be the defect BD-017 and the executor exist to prevent. **(8) Nothing a notification writes carries a secret.** Message bodies, the audit row and the idempotency key's parts go through the binding's redactor (TD-012), and a key that would need redacting is **refused** rather than redacted, for the reason `idempotencyScopeFor` states — redaction is many-to-one and a key is an identity. **(9) Exercised against the Slack fake through the real registration and the real loader**, so the rows, the decryption, the config validation and the redactor composition are production code and only the far side of the HTTP call is a double — the shape `apps/server/src/pipeline.ts:193-199` already describes for the e2e tier. **(10) Needs measurement** (rule 66, not run by the refiner): no tier has ever driven a notification from a pipeline event to a provider call, so the per-event volume a real project generates — and therefore whether an unbatched notification path is usable at all before quiet hours exist — is unknown. **(11) The notification *channel* control is this row's, and the dependency above runs the other way** (added by a refiner after WP-30, session 5). The "Depends on" cell says *"**WP-30** for the wizard's step 4 item 5 and the settings mirror — channel, quiet hours, digest"*; WP-30 shipped and built digest and quiet hours as stored keys and **named the channel as a gap on the screen**, for a reason that is this row's and not that one's: *"A channel is a property of a `communication` binding, and this build resolves `git` and `task_management` only — `CommunicationPort` has no caller anywhere (WP-32) … the channel would be a field whose value nothing could ever read"* (`apps/web/src/features/operating-mode.tsx:29-32`). So the control cannot exist before criterion 1 does, and done includes it: a channel chosen from the project's resolved `communication` binding on `features/operating-mode.tsx` — the **one** component the wizard's step 4 and the project settings page both render — with the gap panel **deleted** in the same change, which is what `apps/server/src/routes/settings-mirror.test.ts` then holds in both directions. Where the value is stored is this row's decision and it is not free: `features.digest` has no channel key today, and a channel is per binding rather than per feature |

## Milestone M3 — show the value
WP-34 shadow mode (closed tickets) + ShadowReport + UI; WP-35 history bootstrap; WP-36 maintenance pipeline; WP-37 risk classes + reviewer routing (CODEOWNERS); WP-38 dependency policy + Checks panel (licence/maintenance status); WP-39 coverage delta; WP-40 epic split (spike variant); WP-41 statistics deep-dive (readiness attribution, clean-first-MR rate, estimate accuracy); WP-42 release 0.1.0 (release-please, changelog, migration notes).

**The nine rows below were written by a refiner in session 5**, from the same reading M2's rows got at `785941f`: each was a one-line item with no acceptance criteria, and each turns out to have a shipped half, a half nobody owns, or both — the shadow *guard* is complete and nothing ever sets `tasks.mode = 'shadow'`, the coverage number is on three schemas and no delta exists, `dependency_policy` ships with a default and no reader, and `readCodeowners` and `listMergedMergeRequests` were built at WP-09 and have never been called. **Recommended order** (dependencies, then cost): **WP-37 → WP-39 → WP-38 → WP-34 → WP-35 → WP-36 → WP-40 → WP-41 → WP-42**. WP-37 and WP-39 are the two cheapest and each gives the Checks panel an item, so WP-38 builds that panel once rather than three times; WP-34 is the milestone's headline and the one row that needs the production runner path (Q52) exercised; WP-41 must come after WP-34, WP-36 and WP-29, because three of its inputs are **wrong** today (PROGRESS backlog **57**, **75**, **63**) and a statistics row that lands first publishes the defect as a number; WP-42 is last and additionally waits on **WP-33**, M2's remaining row.

**Two backlog entries have no home in the nine rows and are not silently attached to one.** PROGRESS backlog **78** (nothing opens the Socket Mode connection the shipped Slack manifest selects, so every inbound chat decision reaches no door) is **not** an M3 feature at all: it completes WP-32's journey, its own entry says *"this is not a backlog-sized change — composition, a lifecycle, a `ROLE` decision and an e2e over an injected socket — so if it is scheduled it wants a **row of its own**"*, and that row is **WP-43** at the next free number, with backlog 78 as its brief and backlog **79**'s remaining half riding with it. PROGRESS backlog **68** (the take-over export writes a tarball nothing serves and no transcript at all) is likewise none of these nine — it is **not** WP-41's CSV export, which is a different artefact — and its own entry says it is the same change as backlog **70** (nothing in the SPA calls take-over or hand-back), so the two together are **WP-44**: one route over WP-15h's existing transcript projection, one authenticated download behind WP-15j's realpath guard, and the screen that gives both a caller. Neither is scheduled here; both are named so that the next orchestrator does not have to re-derive that no M3 row owns them.

| WP | Title | Depends on | Acceptance criteria |
|---|---|---|---|
| WP-34 | **Shadow mode on closed tickets, the ShadowReport and the Shadow screen — the guard is complete and the producer does not exist.** Everything that *refuses* a shadow task's writes shipped long ago: `taskModeSchema` is `('normal','shadow')` (`packages/contracts/src/common.ts:133`), `assertMutatingActionAllowed` throws `shadow.no_outbound_mutation` (`packages/domain/src/aggregates/task.ts:713-720`), `IntegrationActionExecutor` parses `mode` and returns `would_have` before the idempotency step (`packages/application/src/integrations/action-executor.ts:328`), and `runModeFor` maps `shadow` from `tasks.mode` (`packages/application/src/pipeline/planner.ts:555-562`). **Nothing ever sets it**: a grep for `mode: 'shadow'` over `packages/` and `apps/` returns **one** non-test match and it is a badge in the SPA (`apps/web/src/features/task-detail.tsx:450`); `createTaskRequestSchema` carries `mode: taskModeSchema.optional()` (`packages/contracts/src/api.ts:354-358`) and **no route consumes it** — `POST /api/projects/:project_id/tasks` does not exist, and `apps/server/src/routes/projects.ts:285` is the `GET`. `shadow_reports` has existed since `0008_knowledge.sql:122-127` and has never held a row. `shadow.report.created` is `'unconsumed', // WP-34 shadow mode` (`packages/application/src/events/consumption.ts:230`). **The report shape is four fields short of its own specification**: `shadowReportDataSchema` (`packages/contracts/src/artifacts.ts:264-279`) has `ticket`, `human_mr`, `agent_diff_stats`, `overlap.{files_jaccard,size_ratio}`, `agent_review_of_human_mr`, `predicted_cost` and `notes`, while product/19:132 also names *"tests added ratio"*, *"acceptance criteria the human MR covers vs the agent's"*, *"reviewer minutes estimate (from MR events)"* and *"predicted cost **vs shadow cost**"*, and there is no shape at all for its second sentence — *"Aggregate report per shadow batch: predicted cost per ticket by size, similarity distribution, list of 'high similarity + low cost' tickets as the launch candidates"*. **The dial's two Observe policies are this row's, and backlog 72 (c) says so**: `AUTONOMY_POLICY_READERS.picksUpNewTickets` and `.shadowMode` are both `owner: 'none'` (`packages/domain/src/policies/autonomy.ts:451-455,481-485`) with the reasons *"today `intake_check` asks the ticket label and the WIP limits and nothing else"* and *"shadow mode has no runner in this build; `tasks.mode` is chosen by whoever creates the task"* — *"the only pairing in this entry that is a single piece of work"*. **The screen says it out loud**: `apps/web/src/features/operating-mode.tsx:150-158` ships the card with the caveat *"The switch and its budget are stored; nothing in this build starts a shadow batch, so turning it on changes no behaviour yet."* **There is no Shadow screen**, although product/10:20 names one per project: `apps/web/src/features/` has fifteen screens and none is it. **The separate budget has a key and no enforcement carrier**: `features.shadow_mode.budget_usd` (`packages/contracts/src/config.ts:292-297`) against `budget_scope` = `('org','project','task','run')` (`0002_enums.sql:119`) and a guard that reads org and project only (`packages/application/src/cost/guard.ts:82`) — `operating-mode.tsx:519-524` already states the gap in prose. **What it costs to leave**: product/19:162's dogfood **Phase A** is *"onboarding + shadow mode on 10 closed tickets each; success = install < 1 hour, discovery draft accepted with ≤ 30 min edits, shadow similarity reported"* — the phase cannot start, and product/18:13's level 0 (**Observe**) is a dial position that runs nothing, which is the entry point the whole adoption argument rests on | WP-15g (the production `ClaudeRunner` and the workspace provisioner — with none composed, `startRuntime` installs `unavailableClaudeRunner` and a batch cannot run at all), WP-15c, WP-19 (the ledger, the admission guard and the task cap every shadow run must pass), WP-21 (the one-off-task precedent), WP-30 (the materialised preset this row's two policies are read from), WP-09 (`getMergeRequestDiff`, `listMergedMergeRequests`). PROGRESS backlog **72 (c)**. **Q52** for the run workspace, and backlog **71** (a run's workspace has no way to say which branch to check out) if the batch checks out a historical base. **Blocked on human: Q82** — what a shadow run is checked out against and how the human merge request for a closed ticket is found; filed with a recommendation strong enough to build from. Node, Postgres and a runner | **(1) A shadow batch is tasks, and there is no second entry point.** One `tasks` row per selected ticket on the ordinary ticket templates with `mode = 'shadow'`, so the admission guard, the cost ledger, the transcript sink, the budget cap and the escalation are the ones every run gets (`packages/application/src/onboarding/discovery.ts` carries the argument). The batch command matches WP-15i's shape: strict snake_case body, `Idempotency-Key` performing nothing twice asserted on a **countable** effect (N tasks, one `human_actions` row), 401 per route, one wrong-role 403 — and it is **named positively in a test of its own**, because until the screen exists the client-driven census is blind to it by construction. **(2) Nothing external happens, asserted as a count** (rule 79): a shadow task walked through a whole template issues **zero** mutating provider requests and leaves one `integration_actions` row per refused mutation recorded `would_have`, read back from the table rather than from a port's return value. The guard exists; what this criterion asserts is that the batch actually goes through it. **(3) Observe means Observe, both ways** (rule 42): `picksUpNewTickets` is read at `intake_check` and refuses to create a task for a new ticket on an Observe project **and** creates one on a Supervised project; `shadowMode` decides whether a batch may start at all. Both `AUTONOMY_POLICY_READERS` entries flip from `owner: 'none'` to `kind: 'read'` in the same change, and that table's existing test is the recurrence guard. **(4) The report is product/19 §13's, and a field that cannot be computed is absent rather than invented** (rule 16, and WP-15h's `/context-pack` precedent — summing rows into a budget publishes a fact): the four missing fields are added to `shadowReportDataSchema`, or each omission is named in its docblock with the reason it cannot be answered, and `pnpm schemas` is regenerated. **(5) The batch report exists** and is a projection over the batch's `shadow_reports` rows — not a fifth stored number to keep in step — carrying product/19:132's cost-by-size, similarity distribution and the *"high similarity + low cost"* list. **(6) `shadow_reports` gets its first writer and `shadow.report.created` its first consumer**, one writer each, `EVENT_CONSUMPTION` flipped and `sweepReadiness` still satisfiable for a sweeping `ROLE`. **(7) The separate budget stops a batch**, asserted end to end: either the admission guard learns the feature cap or a forward-only migration adds a `budget_scope` value and the settings write creates the row; read back from `cost_entries`, never from a status code, and `operating-mode.tsx:519-524`'s sentence corrected in the same change. **(8) The Shadow screen** (product/10:20) renders a batch's comparisons and its aggregate, with the human MR's title, the ticket's words and the model's notes as React text nodes through `apps/web/src/ui/untrusted.tsx` and every URL through `safeHref` (BD-022); the new client paths join `apps/server/src/routes/client-census.test.ts` in both directions. **(9) `runs.mode = 'shadow'` is asserted rather than assumed** — the planner maps it already and no test in any tier has driven a shadow task through it. **(10) Rule 83**: the feature card's caveat is **deleted** (`operating-mode.tsx:157`, which is backlog 72's adjacent note for all three stored-and-unread feature keys), the two `AUTONOMY_POLICY_READERS` `why` strings, `consumption.ts:230` and backlog 72 (c) are corrected. **(11) Needs measurement** (rule 66, not run by the refiner): no shadow task has ever run, so product/18:24's *"~$5–15 per ticket"*, the wall time of a ten-ticket batch and what that batch does to the WIP scheduler are all unknown |
| WP-35 | **History bootstrap — the read was built at WP-09 and has never been called, and the wizard has no step for it.** product/06:34-35 is step **3b** (*"Offer to mine the last N merged MRs (default 200) with their review comments and closed tickets … Results are KB proposals with provenance (MR links) in the proposal queue, never applied silently. Shows an estimated cost before running"*) and the wizard renders five steps and no 3b (`apps/web/src/features/onboarding.tsx:140,250,312,319,344`). product/19 §18 is the full specification and nothing in it is ambiguous: *"last N merged MRs (default 200, max 1 000) with discussions and diff stats; closed tickets of the last 6 months (titles, descriptions, resolution comments); commit messages. Batches of ~20 MRs per Sonnet 5 run with a fixed extraction schema: recurring reviewer requests → rules candidates; conventions observed ≥ 3 times → `conventions.md` entries; pitfalls (MRs with ≥ 3 review rounds) → lessons; glossary terms; module ownership hints. Every proposal cites MR/ticket links as evidence. Budget cap default $20, shown before start. Output goes to the proposal queue only."* **The port half is half there.** `listMergedMergeRequests(project, since, limit)` exists (`packages/application/src/ports/integrations/git-provider.ts:504-508`, built at WP-09, listed by product/08:10 as *"history export for bootstrap"*) and a grep over `packages/application`, `packages/infrastructure` and `apps/` finds the declaration and **no caller**; `MergedMergeRequest` carries `ref`, `author`, `merged_at`, `title`, `diff_stats` and `discussion_count` (`:155-162`) and **not the discussions**, so *"with their review comments"* is `listDiscussions` once per merge request — `1 + N` provider reads at N = 200, the shape backlog **64** already records at N = 10. The closed-ticket half has `matchTickets(rule, {since, limit})`; **commit messages have no read at all** — `GitProviderPort` has no `listCommits`. **There is no role and no artifact**: `packages/prompts/roles/` has eleven directories and none mines history, `agent_role` has ten values (`0002_enums.sql:34-45`), and the nearest artifact precedent is `librarianProposalsDataSchema` (`packages/contracts/src/artifacts.ts:258-262`), which is already proposals + health + summary. **The `source` value this row is named after is taken**: `knowledge_proposal_source` has `'bootstrap'` (`0002_enums.sql:134`) and exactly one writer, the **Discovery** recorder (`packages/application/src/onboarding/record.ts:228`, whose comment reads *"this one came from onboarding, not from a retrospective"*), so a reader of the queue cannot tell a drafted `technical/*` page from a mined convention. **The budget cap has no key**: `featuresConfigSchema` carries `ticket_linter`, `review_only`, `maintenance`, `digest`, `shadow_mode` and `ask` and nothing for bootstrap, unlike shadow and maintenance which at least have one. **What it costs to leave**: product/18:27's value is *"KB starts at 'week six', not empty"*, product/17's ladder and product/06's completeness score both rest on a populated vault, and backlog **16** records that every retrieval number this repository has is a fixture-vault claim. Nothing is broken today — this is the latent kind, and the trigger is the first real onboarding | WP-18b (the proposal queue, the curator and the apply policy — the output surface), WP-21 (the one-off-task shape, the `onboarding.discovery` job and the wizard), WP-16 (the KB and the indexer), WP-09 (the two reads), WP-19 (the cap), WP-15g (a production runner). Backlog **64** (the paths-only read) if the discussion fan-out is narrowed, backlog **45** (the business interview, the *other* unbuilt wizard step) by adjacency only — it does not block. **Blocked on human: none** — product/19 §18 gives N, the max, the window, the batch size, the extraction schema and the cap. Node, Postgres and a runner | **(1) The mining is a task, batched, through the one entry point.** `runs.task_id` is `not null`, so a bootstrap is a one-off task whose stages or jobs cover ~20 merge requests each, and every run passes the admission guard, the ledger, the transcript sink and the budget cap. N defaults to **200** and is refused above **1 000**, asserted at the value and one past it (rule 42), and the batch size is stated in the change rather than left to a reader (rule 63). **(2) Every input is untrusted stored external text.** Merge-request titles, discussion bodies, ticket descriptions and any commit messages reach the model **only** inside an `assemblePrompt` data block with the per-prompt nonce, and are bounded and redacted at the write the way `tasks.ticket_snapshot` is (TD-012, BD-022) — this is the next place the platform keeps somebody else's words after `inbox`, `kb_chunks`, `ticket_snapshot` and `notifications`, and the byte budget is **derived at the cap** rather than chosen (rule 63, Q54). **(3) Every proposal cites evidence that resolves**, and a proposal with none is **refused** rather than stored unevidenced — product/19:158's *"Every proposal cites MR/ticket links as evidence"* is a precondition of the queue, not a nicety. **(4) Output is the queue only, whatever the apply policy says.** Asserted with `policies.knowledge_apply.auto_apply: true`: a bootstrap proposal is still `queued` — product/06's rule, and the one thing a bulk import must not inherit from the per-task path. **(5) A mined page is distinguishable from a discovery draft** in the queue: either a new `knowledge_proposal_source` value by forward-only migration (TD-011, a new numbered file) or the distinction carried in `evidence`, decided in the change and rendered on the proposals screen. **(6) The provider read volume is stated and bounded**, with the number in the change: N merge requests is `1 + N` calls unless the discussions are batched, the executor's rate limit is named, and backlog **64**'s remedy — ask for the fields you need — is considered and answered either way. **(7) Commit messages are built or scoped out in writing**: either `GitProviderPort` gains the read **with its fake, its contract-suite case and a recorded fixture with a `source` block in the same change** (rule 23, the fixture-provenance suite), or product/19 §18 is amended to drop them (docs win, rule 8). **(8) The cost is capped and shown before start**: a configuration key for the cap, the estimate rendered in the wizard step, and a bootstrap that **stops** at the cap read back from `cost_entries`. **(9) The wizard gains step 3b and the settings mirror gains the same control** (product/18:55), held in both directions by `apps/server/src/routes/settings-mirror.test.ts`. **(10) Rule 82**: the fake produces the merged merge requests, their discussions and the closed tickets the extraction folds; a seeded `kb_proposals` table certifies nothing about the fold |
| WP-36 | **The maintenance pipeline — a stored toggle, a scheduler that does not exist, and an aggregate technical/02 defines with no table.** product/18:31 is *"Scheduled chores within a dedicated budget: dependency bumps, flaky-test hunting, docs drift, lint debt, KB hygiene; each produces a normal `chore` task"*, product/04:120 the same in one line. **The configuration ships and nothing reads it**: `features.maintenance.{enabled, schedule (daily\|weekly\|monthly), budget_usd, chores (deps\|flaky\|docs\|lint\|kb)}` (`packages/contracts/src/config.ts:251-258`), and the wizard card says so on the screen — `apps/web/src/features/operating-mode.tsx:167-175`, caveat *"Stored; no scheduler runs a maintenance batch in this build."* **technical/02 defines two things with no storage**: a **ScheduledJob** aggregate at `:32` (*"project, schedule, chore type, budget, last run"*) and a **MaintenanceScheduler** saga at `:196` (*"creates chore tasks on schedule within budget"*), and a grep for `scheduled_job` over `docs/technical/03-data-model.md` and `packages/infrastructure/src/db/migrations/` returns **nothing** — so whether the schedule needs a table at all is this row's decision, with two working precedents that need none (`registerPartitionMaintenance`, and the price-list job at `packages/infrastructure/src/cost/price-list-maintenance.ts:36`, both in TD-004's own *"index rebuilds, maintenance schedules"* family, `packages/application/src/ports/jobs.ts:337,354,414`). **A maintenance chore has no ticket and `tasks` requires one**: `ticket_provider`/`ticket_key`/`ticket_url` are `not null` (`0004_pipeline.sql:6-8`) under `UNIQUE(project_id, ticket_key, mode)`, and WP-24 and WP-25 each answered it with a platform-issued key (`mr!<iid>`, `lint!<key>`) — which is backlog **62**, *"the write half of 'a platform-issued reference is not a ticket' is refused by name and the read half is not"*, so a maintenance task will ask Jira for a ticket nobody issued unless that entry is paid here. **The product answer to *does it file a ticket* is in the documents and is no**: product/19:126's feature card reads *"Maintenance — 'Weekly chores: dependency bumps, flaky tests, docs drift' · off · budget you set · **MRs**"* and the shipped card copies it (`operating-mode.tsx:173`, `touches: 'opens merge requests'`), while product/08:9's task-management contract names only *"create follow-up tickets"* (the scope-creep valve). The `createTicket` docblock at `packages/application/src/ports/integrations/task-management.ts:127` lists *"maintenance chore"* among its callers and is therefore **wrong against the product documents** — a docblock correction, not work. **`runs.mode` is wrong for the two run kinds every chore already produces** — backlog **57**'s remaining values: `RUN_MODE_BY_TEMPLATE` (`packages/application/src/pipeline/planner.ts:450-453`) is keyed by *template* while `retrospective` and `librarian` are *stages* of all three ticket templates, so six of the 24 rows the existing walk pins (`planner.test.ts:533-589`) record `normal` today. A maintenance pipeline is the first thing that makes *"what did delivery cost, what did upkeep cost"* a question anyone asks of that column, so it is this row's to pay. **The dedicated budget has the same missing carrier as WP-34's** (`features.maintenance.budget_usd` against `budget_scope` and `cost/guard.ts:82`; `operating-mode.tsx:519-524`). **The five chore types are five different pieces of work and only one is cheap**: `deps` needs WP-38's metadata, `flaky` needs the flaky detection product/04:57 says *"is not implemented"*, `docs` needs drift detection (`policies.drift_without_direction` exists at `effective-config.ts:91` with no reader), `lint` needs a project command a run may execute — **which backlog 49 says no run of any role can do on this build**, Q69 being its product half — and `kb` is WP-18b's nightly hygiene, which already runs. **What it costs to leave**: product/18:31's value is *"Keeps the repository agent-ready; visible in readiness trend"*, and backlog **46** already records that readiness is evaluated once, at discovery. A toggle is stored, a card is rendered, and nothing happens | WP-15 (the chore template and the interpreter), WP-05 (cron and singleton/coalesced jobs), WP-19 (the budget and the admission guard), WP-21 (the one-off-task shape), WP-30 (the card and the stored keys), WP-18b (the `kb` chore, which already runs nightly). Backlog **62** (the platform-issued reference's read half), **57** (the three `runs.mode` values), **49** / **Q69** (a project command a run may execute — which decides whether `lint` can ship at all), **46** (readiness is evaluated once). **Blocked on human: none** — product/19:126 and product/18:31 decide the external touch (merge requests, not tickets) and the defaults; which of the five chore types ship is an engineering scope this row states. Node and Postgres | **(1) One scheduler, idempotent, and its storage decided in writing.** The schedule fires through the Jobs port's singleton/coalesced family and a double fire creates **one** task, asserted by counting rows (rule 79); whether it lives in pg-boss cron or in a `scheduled_jobs` table is decided here and technical/02's ScheduledJob line is implemented or amended (docs win, rule 8). The cron carries its own timezone and an injected clock, never a wall clock (rule 2, and the precedent `price-list-maintenance.ts:36` states). **(2) A maintenance chore is an ordinary `chore` task** — same interpreter, gates, iteration limits, WIP scheduler, escalation and workpad — with no second pipeline and no new task state. **(3) The ticket question has one answer and it is the documents'**: the chore runs on a **platform-issued reference** and files no ticket, backlog **62**'s read half is closed in the same change so no maintenance task asks a provider for a key nobody issued, and `task-management.ts:127`'s docblock is corrected (rule 83). **(4) `runs.mode` stops lying for `retro` and `librarian`** — backlog **57**'s remaining values — through a stage-keyed lookup beside `RUN_MODE_BY_TEMPLATE`, with the rule for which key wins when both match stated at the line, the existing 24-row walk extended so a later template cannot quietly take `normal` (rule 18), and the backfill question (`runs.mode` is written once and nothing rewrites it) answered in the same commit. **(5) The dedicated budget stops a batch**, asserted end to end from a real HTTP or configuration write, not a seeded row (rule 82) — and WP-34 and this row must not build **two** mechanisms: whichever lands first builds it and the other asserts it. **(6) Only the chore types this build can perform are offered, and the rest refuse by name** (rule 18: the absent case must not be the quiet one) — `lint` in particular states its dependence on backlog **49** / Q69 rather than being accepted and silently doing nothing. **(7) The card's caveat is deleted in the same change** (`operating-mode.tsx:174`; backlog 72's adjacent note), and every sentence this falsifies is found by grep (rule 83). **(8) Driven in the e2e tier from the scheduler's own fire to a merged chore**, with a fake that picks its scenario from the spec rather than from `spec.stage` (rule 82) — a scheduled chore has no ticket text to dispatch on. **(9) Needs measurement** (rule 66, not run by the refiner): nothing has ever released a batch of scheduled tasks against a project's WIP limits, so what a weekly batch does to the queue, and whether a chore's budget is spent by one task or ten, is unknown |
| WP-37 | **Risk classes and reviewer routing — the gate has a reader and has never had a producer; PROGRESS backlog 73 is this row's brief and is not restated here.** Four symptoms, one cause, all read off the tree at WP-30. **(a)** No default classes ship: `PLATFORM_DEFAULT_CONFIG.policies` (`packages/domain/src/config/effective-config.ts:82-104`) carries `protected_paths` and **no `risk_classes` key**, so WP-30's gate (`riskClassesRequiringPlanApproval`, called from `packages/application/src/pipeline/saga.ts:678`) is dead on every project and the settings screen renders the empty state (`apps/web/src/features/operating-mode.tsx:462-467`), while product/19 §14 tables **six** classes — auth, payments, data, infra, agent-config, public-api — with path patterns and a default policy each. **(b)** The proposal has no producer and no field to return one in: `discoveryDraftDataSchema` (`packages/contracts/src/artifacts.ts:300-320`) is a strict object with `documents`, `commands`, `linked_documents`, `questions` and `readiness`, against product/18:52's *"Risk classes proposed from the repository structure; reviewer routing from CODEOWNERS if present"*. **(c)** `tasks.risk_classes` has no writer and the task page renders it: `packages/infrastructure/src/db/schema/pipeline.ts:93`, published at `packages/contracts/src/records.ts:220`, rendered at `apps/web/src/features/task-detail.tsx:660-673` — every task shows *"none"*. **(d)** Of the three requirements product/19 §14 defines, one is read, one parses with no consumer and one **cannot be written at all**: `riskRequirementSchema` (`packages/contracts/src/config.ts:150-153`) is `z.enum(['plan_approval','budget_approval'])` united with `/^reviewer:@?[A-Za-z0-9._\-/]+$/`, so `checklist:<name>` is **refused by a strict boundary schema** and product/19 §14's *"payments → plan approval + stricter checklist"* and *"public-api → stricter checklist"* are unexpressible — and `packages/domain/src/policies/risk-classes.ts:9`'s claim that `checklist:<name>` *"is parsed … and has no consumer"* is **false**, a comment correction rather than work (rule 83's shape). **The routing half, which backlog 73 names and does not detail, is the same story twice.** `readCodeowners(project, ref)` was built at WP-09 (`packages/application/src/ports/integrations/git-provider.ts:502`) and a grep over `packages/application`, `packages/infrastructure` and `apps/` finds the declaration and the two implementations (`gitlab/provider.ts:800`, `git/fake.ts:1094`) and **no caller**; `MergeRequestUpdate.reviewers` exists (`git-provider.ts:253`) and nothing sets it, although product/08:10 lists `set_reviewers` among the git provider's actions. product/19:138 fixes a three-step precedence — *"CODEOWNERS match first, then project `reviewers` config, then the requesting human as fallback; risk classes add required reviewers rather than replace"* — and **the middle step has no key**: there is no `reviewers` anywhere in `packages/contracts/src/config.ts`, so a strict schema refuses what the document tells an operator to write. The **fallback** needs a mapped identity, and `user_identities` has had a writer only since WP-31 and is empty by default (backlog **79**), so on a fresh instance step three resolves to nobody. **What it costs to leave**: BD-030 and product/19 §14 are the product's answer to *"what stops the agent touching auth and payments unsupervised"*, and the answer on this build is a gate that never fires, a task field that is always empty and a wizard step that says so. **Latent, with a named trigger**: the moment one project hand-writes a class, (c) and (d) go live together and a class the *implementation* touched but the *plan* never named escapes the gate — the residual `risk-classes.ts`'s docblock already states | WP-30 (the gate, the materialised preset and the settings screen), WP-26 (the merge request's own diff, which is the authoritative source product/19 §14 names and which the rebase gate already fetches — `changedPathsOf`, `packages/application/src/pipeline/conflict-warning.ts:180-205`), WP-21 (the Discovery artifact, its prompt, `ROLE_PROMPT_VERSIONS` and TD-016 eval cases), WP-09 (CODEOWNERS and reviewers), WP-28 (the `budget_approval` requirement's natural consumer — its row does not name it, and this one says so). Backlog **73** (the brief), **79** (the identity mapping the fallback resolves through), **64** (the diff read's shape). **Blocked on human: Q83** — what a *"stricter checklist"* is; filed with a recommendation, and it blocks **only** the `checklist:` half, which criterion 5 refuses loudly until it is answered. Node and Postgres | **(1) The six classes ship as a *proposal*, not as silent defaults.** product/18:52 says the wizard proposes them from the repository structure, so `policies.risk_classes` stays empty until somebody accepts, and the acceptance writes the configuration document through the existing `PUT /api/projects/:project_id/config` path with its `human_actions` row. Asserted both ways (rule 42): a project that accepted has the classes and one that did not has none — a change that put them in `PLATFORM_DEFAULT_CONFIG` would silently gate every existing project's migrations on the next deploy. **(2) The Discovery agent can return them**: `discoveryDraftDataSchema` gains the field, the prompt asks for it, `ROLE_PROMPT_VERSIONS` bumps and eval cases land in the same change (TD-016) — and the model's classes are **suggestions**, the way R9/R11/R12 are the platform's own answer in the readiness criteria. **(3) `tasks.risk_classes` gets a writer, and it computes from the *implementation*, not from the plan.** A narrow repository method (never a whole-row `tasks.save`, rule 79 and backlog 18) fed from the merge request's changed paths where WP-26's read already happens, asserted by the countable effect: a task whose diff touches `**/migrations/**` carries `data` on its row, and a task whose diff touches nothing classed carries an empty list. **(4) Reviewer routing is the document's three steps in the document's order**, each asserted separately: a CODEOWNERS match wins; with no match the project's `reviewers` key wins — **which this row adds to `projectConfigSchema`**, because a strict schema currently refuses the middle step; with neither, the requesting human is the fallback, and an **unmapped** identity resolves to nobody and says so by name rather than assigning silently (backlog 79). Risk classes **add** required reviewers and never replace them, asserted with a class and a CODEOWNERS entry that disagree. **(5) Every requirement the schema accepts has a consumer, and one that has none is refused** rather than parsed: `plan_approval` is WP-30's gate, `reviewer:@handle` is this row's, `budget_approval` is WP-28's mechanism and is either wired here or refused by name, and `checklist:<name>` stays **out of the union** until Q83 is answered — with the refusal carrying the key path and the value found there, the shape backlog 58's 409 already uses. **(6) Setting reviewers is a `pipeline.outbound` duty** through `IntegrationActionExecutor`, never from inside a handler's transaction, keyed so a redelivery assigns nobody twice, read back from `integration_actions` (rule 79); a shadow task assigns none. **(7) A handle from CODEOWNERS is untrusted external text** (BD-022): bounded and redacted at the write, rendered through `apps/web/src/ui/untrusted.tsx`, and never interpolated into a provider request without the port's own validation. **(8) Rule 83**: `risk-classes.ts:9`'s false `checklist:` sentence, the empty-state copy at `operating-mode.tsx:462-467`, backlog **73**'s four symptoms and WP-28's row (which does not name `budget_approval` as a risk requirement) are corrected. **(9) Needs measurement: none** — every fact above was read off a file |
| WP-38 | **Dependency policy and the Checks panel — the policy key ships with a default and has no reader, and the metadata it would show has no source anywhere.** product/04:58 puts the gate in the Developer stage — *"adding a third-party dependency follows the project policy (`ask` by default → a question with license and maintenance status; `allow` for allow-listed packages; `block`)"* — and product/18:37 is the same with its configuration column, *"`allow \| ask \| block` per ecosystem; allow-listed packages"*. **The key exists and nothing reads it**: `policies.dependency_policy: z.enum(['allow','ask','block'])` (`packages/contracts/src/config.ts:167`) with default `'ask'` (`packages/domain/src/config/effective-config.ts:90`), and a grep for `dependency_policy` or `dependencyPolicy` over `packages/` and `apps/` returns **three** matches: the schema, the default and one test fixture (`packages/contracts/src/config.test.ts:61`). **The schema is also one value where the document asks for a map**: a scalar enum cannot express *"per ecosystem"* and there is no allow-list key at all, and the boundary schema is strict, so neither can be written. **Nothing can detect a dependency being added.** The only code in the tree that looks at a change's contents is `changedPathsOf` at the rebase gate, which keeps `new_path`/`old_path` and discards every patch (`packages/application/src/pipeline/conflict-warning.ts:180-205`, backlog **64**), and WP-24's `tasks.review_subject` (migration 0020), which is a bounded redacted diff for the Reviewer. A lockfile hunk is the signal and nothing reads one. **The metadata has no producer and no decided source.** The platform ships five integration types (`packages/integrations/src/catalogue.ts`: task management, git, communication, error tracking, logs) and none knows a package registry; nothing in the tree makes an HTTP request to npm, PyPI, deps.dev or Libraries.io; and backlog **48** records that *"a binding's host is whatever the caller typed, and the platform process has no outbound allow-list"*, so adding one is not free. **The Checks panel names this row on the screen and the sentence is already half stale**: `apps/web/src/features/task-detail.tsx:674-678` reads *"CI, rebase status, review threads, coverage delta and dependency status are not on this panel yet: the pipeline that produces them lands with WP-15 and WP-38. They are absent rather than shown as passing."* — WP-15 and WP-26 shipped, so CI and rebase are producible today and the caveat says they are not (rule 83). product/10:38 is the panel's full list — *"acceptance criteria met, CI green, rebase status, review threads open/resolved, business verdict, tamper check, coverage delta, dependency status, risk classes and required reviewers, budget vs estimate, questions pending"* — **eleven** items, of which the shipped panel renders risk classes (always empty, WP-37) and a caveat. **What it costs to leave**: BD-030 names this as one of the additional gates, and unlike most of M3 it is **live rather than latent** — the Developer stage writes code today and may add any dependency in any ecosystem with nothing asking. The panel is product/10's *"merge-readiness at a glance"*, the one screen a maintainer uses to decide whether to merge, and it answers one of eleven questions | WP-15 (the question gate and its timeout, the mechanism `ask` parks on), WP-26 (the diff read and its cost), WP-24 (`tasks.review_subject`, the bounded redacted diff already stored), WP-37 (the panel's risk-class and required-reviewer items), WP-39 (its coverage item — **build the panel once**: whichever of the three lands last owns it, and the recommended order puts that here), WP-28 (budget vs estimate, shipped). Backlog **48** (no outbound allow-list) is a **prerequisite** if the platform makes the metadata call itself; backlog **64** if the diff read is narrowed. **Blocked on human: Q84** — where licence and maintenance status come from, and whether the platform is allowed to call a host no operator configured; filed with a recommendation strong enough to build from. Node and Postgres; a network call only if Q84 is answered that way | **(1) A dependency addition is detected from the change, both ways** (rule 42): a task whose diff adds a package to a manifest or lockfile raises exactly **one** question under `ask`, and a task whose diff touches neither raises **none** — a detector that fired on every diff would pass a one-sided test. The ecosystems it recognises are enumerated in the change and one it does not recognise is stated rather than implied (rule 18). **(2) All three policy values have a distinct, asserted ending**: `allow` proceeds, `ask` parks on the existing question gate with its existing timeout, `block` returns the stage with a named reason; an allow-listed package under `ask` proceeds with no question. Each ending is a countable effect — one `questions` row, one return, zero rows — never a status code (rule 79). **(3) The schema expresses what the document tells an operator to write**: per-ecosystem policy and an allow-list, or product/18:37 is amended (docs win, rule 8). A strict schema that refuses the documented configuration is a defect whichever way it is closed. **(4) Where the metadata comes from is decided, bounded and safe (Q84).** Whatever the answer: it is untrusted third-party text, bounded and redacted at the write (TD-012) and rendered as React text nodes with every URL through `safeHref` (BD-022); if the platform makes the call, the host is on an **operator-declared** allow-list (backlog **48**, and the shape `APP_INTEGRATION_SECRET_ENV` already uses) and the call goes through `IntegrationActionExecutor` from a `pipeline.outbound` job — never from a handler, with `events/open-transaction.ts` refusing the alternative on both paths. **(5) The Checks panel renders what exists and refuses to invent the rest** (rule 16, and WP-15h's `/context-pack` precedent): each of product/10:38's eleven items is either a real projection or listed as **absent with the work package that owns it**, and that list lives **in the test** rather than in prose, so it cannot go stale — the shape `client-census.test.ts` uses. **(6) The stale caveat is corrected in the same change** (`task-detail.tsx:674-678`, rule 83): CI and rebase status ship since WP-15 and WP-26. **(7) Every new client path joins `apps/server/src/routes/client-census.test.ts` in both directions.** **(8) Rule 82**: the fake produces the diff the detector reads and the metadata the panel renders; a seeded dependency-status row certifies nothing about either. **(9) Needs measurement** (rule 66): how often a real Developer run adds a dependency is unknown, so whether `ask` at the shipped default is a gate or a nuisance has never been measured — and that number decides whether the default survives dogfooding |
| WP-39 | **Coverage delta — three schemas already carry the number, the adapter already fills it, and there is no delta because nothing stores a base.** product/18:38 is *"Test coverage change of the MR shown in Checks when the project's CI reports coverage"*, default *"on when available"*, configuration *"coverage source"*. **The head number is in the tree and unread**: `PipelineStatus.coverage_pct` (`packages/application/src/ports/integrations/git-provider.ts:140`), `MergeRequest.coverage_pct` (`:50`), the `ci.pipeline.finished` payload's `coverage_pct` (`packages/contracts/src/events.ts:549`), GitLab fills both (`packages/integrations/src/providers/gitlab/provider.ts:356,756`) and declares `coverageArtifacts: true` (`:273`), and the provider's own docblock names this row twice — *"into WP-39's coverage/diff deltas"* (`:26`) and *"`coverage`, on the pipeline or on a build. `getPipelineStatus` is the read that has it"* (`:41`). **What is missing is the second number.** A delta is head minus base and nothing stores a project's default-branch coverage: a grep for `coverage` over `packages/infrastructure/src/db/` returns **nothing** — no table, no column, no rollup. The read is one call away (`getDefaultBranchHead(project)` at `git-provider.ts:484`, then `getPipelineStatus(project, sha)`), so the work is caching and a staleness rule, not an integration. **`coverage source` has no key**: a grep for `coverage` over `packages/contracts/src/config.ts` returns nothing, and the schema is strict, so a project cannot name one. **A pipeline coverage is one scalar**: per-file coverage needs the coverage *artifact*, which is what `coverageArtifacts` is about and which no code downloads — so *"coverage delta"* on this build means one percentage point, and saying so is part of the work. **The panel says it**: `apps/web/src/features/task-detail.tsx:674-678` lists coverage delta among the absent items. **What it costs to leave**: small and honest — one of product/10:38's eleven Checks items, *"on when available"*. It costs a maintainer the single number that says whether the agent's tests covered the agent's change, and it is the cheapest row in M3 because every piece but one is already built | WP-15 (the CI gate, which already consumes `ci.pipeline.finished`), WP-09 (both reads), WP-38 (the Checks panel — **either order works and the panel is built once**; the recommended order puts this row first so WP-38 inherits an item rather than the other way round). **Blocked on human: none** — product/18:38 gives the default and names the one configuration key. Node and Postgres | **(1) The delta is head minus a *named* base, and the name is in the row.** The project's default-branch head at the time the task branched, or the latest default-branch pipeline: whichever, the choice and its staleness window are stated in the change (rule 63) rather than left for a reader to infer from the code. **(2) A missing number is missing, never zero** (rule 16): a project whose CI reports no coverage renders *"not reported"* and **not** `0.0 %` or `+0.0`, asserted against a `ci.pipeline.finished` carrying `coverage_pct: null` — the failure mode that would read to a maintainer as *"the agent added no coverage"*. **(3) `coverage source` becomes a configuration key** or product/18:38 is amended (docs win, rule 8). **(4) The base is read at most once per task per base sha**, through `IntegrationActionExecutor` from outside every transaction (WP-15d's shape, refused mechanically by `events/open-transaction.ts`), with the cache's key and lifetime stated. **(5) Both directions asserted** (rule 42): a change that raises coverage shows a positive delta and one that lowers it a negative one — a renderer that always printed the head number would pass a one-sided test. **(6) The scalar-versus-per-file limit is stated at the code**, not implied: this build compares one percentage and does not download the coverage artifact, and product/10:38's *"coverage delta"* is met to that extent. **(7) The panel item and `task-detail.tsx:674-678`'s caveat move together** (rule 83). **(8) Rule 82**: driven from a real `ci.pipeline.finished` carrying a number, never from a seeded column — the tier that fakes the CI event must be the tier that produces it |
| WP-40 | **Epic split — the spike template is declared in the contracts and deliberately not shipped, and the acceptance surface the variant needs does not exist.** product/04:117: *"**Spike template** Research/analysis tickets: `Intake → Refinement → Architecture (produces a document instead of a plan) → Human`. Output is a markdown report attached to the ticket and stored in the KB under `research/`. No MR. Variant **epic split** (opt-in): the input is an epic and the output is a proposed ticket breakdown with acceptance criteria for the PM to accept."* product/18:39 adds the default: *"off (spike template option)"*. **The template is declared and refused, with the refusal written down**: `BUILTIN_TEMPLATE_IDS = ['feature','bug','chore','spike']` (`packages/contracts/src/common.ts:346`) against `SHIPPED_TEMPLATES` = feature, bug, chore, discovery, review_only, ticket_lint (`packages/domain/src/pipeline/templates.ts:533-538`), and the docblock states why at `:13-16` — *"`spike` … is in `BUILTIN_TEMPLATE_IDS` and is **not** shipped here: it ends at a human with no MR, so it exercises none of the loop this work package is about, and a template nothing runs is a template nothing tests."* The refusal was right for WP-15 and it is this row that pays it. **Nothing routes an epic to it**: `DEFAULT_TEMPLATE_BY_ISSUE_TYPE` maps `epic: 'feature'` (`packages/application/src/pipeline/settings.ts:83-95`), so an epic ticket is delivered as a feature today. **There is no artifact shape**: `ARTIFACT_SCHEMAS` (`packages/contracts/src/artifacts.ts`) has no ticket-breakdown type; the nearest is `RefinedSpec`. **There is no acceptance surface**: `approval_kind` is `('plan','budget','knowledge','rework')` (`0002_enums.sql:94`) and none of them means *"the PM accepts a breakdown"*, while the other acceptance queue — `kb_proposals` — accepts **pages**, not tickets. **The write itself is the largest external action this platform makes.** `TaskManagementPort.createTicket` exists with a capability flag (`packages/application/src/ports/integrations/task-management.ts:220`, `TaskManagementCapabilities.createTicket`) and its docblock names this row: *"A ticket the platform creates: scope-creep valve, epic split, maintenance chore (product/08)"* (`:127`) — product/08:9's contract says *"create follow-up tickets"* — and Jira's adapter already refuses in shadow mode with an obviously fake key (`packages/integrations/src/providers/jira-cloud/index.ts:796`, *"a shadow task must not be able to pretend it filed a ticket"*). The epic is already within reach: `Ticket.epic` and siblings come back from `readTicket` (`task-management.ts:88`) and `ticket.matched` carries `epic` (`packages/contracts/src/events.ts:125`). **What it costs to leave**: nothing today. It is the cheapest M3 row **not** to do and the most visible one to do badly — creating N tickets in somebody's tracker is the largest write this platform makes into another team's tool (A3's shape, and the reason every other outbound default in this build is conservative). It is also the one M3 feature aimed at the person who *writes* the tickets rather than the person who reviews them, which is where product/19 §17's linter says the failures start | WP-15 (the interpreter and the template shape), WP-17 (the `product_manager` role, which exists at `packages/prompts/roles/product_manager/` with prompt and eval cases), WP-08 (`createTicket` and its capability flag), WP-15i (the command shape an acceptance uses), WP-19 (the budget a spike run is charged against), WP-15f (the ticket's own words — an epic split reads a description, and a row that started before it would split a key). **Blocked on human: Q85** — where a proposed breakdown is accepted and whether accepting creates real tickets; filed with a recommendation. Nothing else in M3 depends on this row, and it depends on nothing in M3. Node, Postgres and a runner | **(1) The spike template ships and is tested**, in product/04:117's shape — intake, refinement, architecture producing a document, then a human, no merge request — and the refusal at `templates.ts:13-16` is corrected in the same change (rule 83). **(2) An epic reaches it**: `DEFAULT_TEMPLATE_BY_ISSUE_TYPE`'s `epic: 'feature'` is decided rather than left — either the epic type maps to `spike` when the variant is on, or the variant is selected some other way and the map stays, stated in the change. **(3) The breakdown is a strict artifact with eval cases**, `ROLE_PROMPT_VERSIONS` bumped in the same change (TD-016), and its acceptance criteria are the model's words in a data block, never the platform's. **(4) Nothing is created until a human accepts.** The acceptance is a command on WP-15i's shape — strict snake_case body, `Idempotency-Key` performing nothing twice asserted on a **countable** effect (N child tickets, one `human_actions` row), 401 per route, at least one wrong-role 403 — and the acceptance's storage is decided (Q85): a new `approval_kind` value by forward-only migration, or a proposal queue of its own. A change that creates tickets on the run's own verdict fails this criterion outright. **(5) Ticket creation is a `pipeline.outbound` duty** through `IntegrationActionExecutor`, never from a handler's transaction, keyed per child so a replay creates each one **once**, read back from `integration_actions` (rule 79); a shadow task creates none, asserted. **(6) A binding without the `createTicket` capability refuses by name** rather than producing a breakdown nobody can accept — the port's own capability contract (*"a provider that is asked anyway throws `IntegrationUnsupportedError` rather than pretending"*), asserted with a read-only binding. **(7) Every word that reaches the tracker is model output over untrusted input**: bounded and redacted at the write (TD-012, BD-022), with the bound derived at the cap rather than chosen (rule 63). **(8) Off by default** (product/18:39) with the default asserted **and** the override asserted (rule 42). **(9) Driven end to end with a fake that picks its scenario from the prompt rather than from `spec.stage`** (rule 82) — a spike's stages are not the ticket templates', so a stage-dispatching fake would certify the wrong thing |
| WP-41 | **The statistics deep-dive — the reader of seven signals nothing consumes, and the row three backlog entries say must not be the one that discovers them.** The screen is an honest stub and names this row six times: `apps/web/src/features/statistics.tsx:1-12` — *"no rollup table is populated yet (WP-19 builds the cost ledger and rollups; WP-41 the deep dive) … a screen full of zeroes reads as 'we delivered nothing' rather than 'nothing is measured yet'. Recorded as Q45"* — with `PLANNED` (`:17-28`) listing six numbers and their owners. `GET /api/org/stats` has **no route, no DTO and no rollup**, and **Q45's statistics half is still open** (`docs/OPEN-QUESTIONS.md:99`): *"`GET /api/org/stats?range=…` has nothing at all — no rollup DTO, no CSV shape … decide whether the statistics DTO belongs in WP-19 (which computes the rollups) or WP-41 (which defines the deep-dive metrics)"*. The client-driven census is blind to the absence by construction, because the SPA names no such path. **Seven signals are declared unconsumed with this row's number on them**, all in `packages/application/src/events/consumption.ts`: `mr.updated` (`:176`), `task.review.observed` (`:219`, *"product/18's accepted-vs-dismissed"*), `task.lint.posted` (`:220-223`, whose other half — *"edited within 48 h"* — needs a ticket-changed signal no normaliser produces, backlog **59**, unowned), `task.rebase.checked` and `task.conflict.warned` (`:224-229`, *"conflicts auto-resolved vs escalated"* and *"concurrent-task overlaps"*), plus `shadow.report.created` (`:230`, WP-34's, whose aggregate lands here) and `human_time_entries`. **That list was read at `7c25024` and one entry of it is already moving**: WP-29 is in flight as this row is written and its own criterion 4 corrects `mr.updated`'s `// WP-41 statistics` comment if its projector folds the event, so read `EVENT_CONSUMPTION` before quoting this list rather than quoting it (rule 83 — the sentence nearest the fix is the one nobody re-reads). **Update (refiner, session 5): WP-29 has landed and `human_time_entries` holds rows.** The projector is `packages/application/src/human-time/projector.ts` at TD-005 priority 230 and the **only reader in the build** is the task-detail projection (`apps/server/src/queries/pipeline-queries.ts:657-671`, `human_time` on the task DTO), so the per-project, per-user, windowed read product/16:29 asks for is still this row's and still has no route, no DTO and no rollup. Three backlog entries came out of that work and are this row's **inputs rather than its discoveries**: **88** (a bot that is not this platform opens and extends a review window, so reviewer minutes over-count — `user_identities.user_id` is `not null`, so the flag is a migration, not a column), **90** (`mr.approved` is in product/08:10's contract and absent from the catalogue, so the same minutes under-count by exactly the reviewers who approve without commenting — the two errors run in opposite directions and do **not** cancel), and **89** (the 8 h day cap is applied per entry, so a cross-task per-user total can exceed the cap this row's own tooltip would quote; the read-side cap, if it is wanted, is **this row's**, and the projector stays per entry either way because a fold-time cap would break criterion 5's replay equality). WP-29 built `human_time_entries`, which product/16:29 turns into *"Human minutes per merged MR and total cost of delivery (tokens + people)"* — with **Q73** open on the rate, so minutes and dollars stay two fields. **Three backlog entries say in their own words that this row must not go first.** **57**: *"any statistic or cost breakdown grouped by mode is wrong by construction … WP-41 must not be the first row to discover this, because it would then be measuring the defect"* — `runs.mode` is `normal` for `retro`, `librarian` and `discovery`, all of which have live producers. **75**: `tasks.cost_estimated` is `not null default 0` with no writer, *"so every task the product has served reports `$0.00` of estimated spend"*, and *"**WP-41** … must not be the row that discovers this"*. **63**: the conflict badge the board has no field for — *"Cheapest owner is **WP-41** … which already owes the only consumer of both WP-26 events and would traverse the same stream once for both"*, so that entry **is** this row's, while **81** names WP-41 by adjacency and rules it *"the wrong home"* (*"it counts delivery performance rather than platform health"*), so that one is **not**. **Two headline metrics have no event to rest on.** product/16:15's *"Clean-first-MR rate per ticket author"* needs a ticket author `ticketRefSchema` does not carry (**Q48**) and a mapping `user_identities` holds only once an admin has stated one (backlog **79**); product/16:16's *"Reviewer minutes per merged MR"* starts at *"the first human MR activity (comment, approval, review start)"* and technical/02:130-131's catalogue has **no `mr.approved` and no review-requested type**, although product/08:10 lists `mr.approved` among the git provider's events — a disagreement between a product document and the event catalogue that becomes a wrong number here. **Readiness attribution**, this row's own line, rests on `readiness_evaluations`, which has one writer since WP-21 and backlog **46** records that readiness is evaluated **once**, at discovery, against product/17's re-check after every merged task — so an attribution over a single evaluation per project is a constant. **What it costs to leave**: product/16 is the product's own definition of success and product/10:22 puts the screen in the org navigation; a visitor is currently taught that nothing is measured. Every unread event above accrues history whose meaning nobody re-derives later — the shape backlog **52** had for `human_actions` — and three of the inputs are wrong *today*, so a row that built the screen first would publish the defects as numbers | WP-19 (the ledger and `cost_rollup_daily`), WP-29 (the minutes; **Q73** open, so minutes and USD are never summed), WP-34 (shadow similarity and predicted cost), WP-36 (which pays backlog **57**'s `runs.mode` values), WP-24 / WP-25 / WP-26 (the three metric events already emitted and unread), WP-15h (the read-API shape, the projections' refusal rule and the census). **Blocked on human: Q45's statistics half** (where the DTO lives and what the CSV is) and **Q73** (the hourly rate). Backlog **75** (unowned, one projection) and **63** (this row's) must be paid **in** this row or before it; backlog **81** is explicitly **not** this row's. Node and Postgres | **(1) `GET /api/org/stats` has a published DTO in `packages/contracts` and a CSV shape** (product/10:22), Q45's remaining half answered in the change, `pnpm schemas` regenerated; the route is asserted by a test of its own until a screen calls it, and by both directions of the census once one does. **(2) Every number is defined where it is published** (product/10:63: *"Every number shown has a tooltip with its definition"*), and a number this build cannot compute is **absent, with the work package that owns it**, never a zero (rule 16). **(3) The three wrong inputs are paid or excluded by name**: a breakdown by `runs.mode` is refused until backlog **57**'s remaining values are written; `tasks.cost_estimated` (backlog **75**) gets the projection that entry recommends — `sum(usd) where is_estimate` over `cost_entries`, no fourth stored number — or is excluded explicitly; the conflict metrics carry backlog **63**'s board field and **64**'s and **65**'s stated residuals. **(4) Each signal consumed flips in `EVENT_CONSUMPTION`** with a declared TD-005 priority, `sweepReadiness` stays satisfiable for a sweeping `ROLE`, and every type still unconsumed names the work package that will flip it (rule 18, rule 83). **(5) The projections are backfillable**: registered through `events/replay.ts` so a replayed range of the append-only `events` table produces rows **equal** to a live dispatch's — an equality, not a spot check — and a second pass is a no-op, exactly as the cost ledger's backfill is. **(6) Per-user breakdowns default off** (product/18:32, asserted both ways) and an unmapped identity is a null the totals survive; a per-user, per-day total either applies backlog **89**'s cap at read time or states at the number that the cap is per task, and any reviewer-minutes figure names backlog **88**'s and **90**'s error directions rather than publishing them silently. **(7) Two metrics that cannot be computed say so rather than approximating**: the clean-first-MR rate waits on Q48's ticket author, and reviewer minutes wait on an `mr.approved` the catalogue lacks (backlog **90**, which prices it at **one** normaliser — GitLab is the only `type: 'git'` provider this build registers) — each either gets its event through a normaliser change **with its fake, its contract-suite case and a recorded fixture in the same change** (rule 23), or is listed as absent with the reason. **(8) Rule 82**: driven from real events in the integration or e2e tier, never from seeded rollup rows — a tier that seeds the rollup certifies nothing about the fold. **(9) Rule 83**: `statistics.tsx:1-12`'s docblock, its `PLANNED` table, Q45's closing sentence and the four `// WP-41` comments in `consumption.ts` are corrected in the same change |
| WP-42 | **Release 0.1.0 — TD-019's release mechanism has never been built, and the one rule that protects an upgrade has a function and no caller.** TD-019 decides release-please v5 (a release PR a human merges, tags `vX.Y.Z`, commitlint, image tags `X.Y.Z`/`X.Y`/`X`/`sha-<7>`/`edge`, never `latest`) and technical/11:25 names the workflow: *"`release.yml` \| `push: main` \| release-please release PR; on merge: tag → `image.yml`"*. **There is no `release.yml`**: `.github/workflows/` holds exactly three files — `base-image.yml`, `ci.yml`, `image.yml` — and a grep for `release-please` over every YAML, JSON and Markdown file outside `docs/` returns **nothing**. There is **no `CHANGELOG.md`**, no release-please manifest and no configuration. **Every package is `0.0.0`**: the root `package.json` and all ten workspace manifests (`apps/{server,web,launcher,runlet}`, `packages/{contracts,domain,application,infrastructure,integrations,prompts}`), so a decision nothing in TD-019 makes — one product version, or release-please `manifest` mode per package — falls to this row. **technical/11 lists eleven workflows and three exist**, which is mostly absorption rather than absence and the difference matters: `dco` and `commitlint` are `ci.yml` jobs (TD-019's own amendment says so), `integration`, `e2e`, `web-e2e` and `secret-scan` are `ci.yml` jobs too (`.github/workflows/ci.yml:131,147,105,182`), `evals.yml` and `nightly-llm.yml` are **WP-33's** and still TODO, and `codeql.yml` and `mutation.yml` have **no owner at all** — which is a finding this row files rather than absorbs. **TD-019's hygiene list is two thirds shipped**: `CONTRIBUTING.md`, `SECURITY.md`, `.github/pull_request_template.md` and `THIRD_PARTY_NOTICES.md` exist; `CODE_OF_CONDUCT.md`, `.github/CODEOWNERS` and `.github/ISSUE_TEMPLATE/` do not. **The upgrade guard is written and unreachable**: `findUnknownMigrations` (`packages/infrastructure/src/db/migrator.ts:157-174`) is TD-019's *"the app refuses to start when the DB schema is newer than the code"* — its docblock says exactly that — and a grep over `packages/` and `apps/` returns the definition and **no caller**, so an operator who rolls an image back starts it against a schema 0024 it does not know. It is **latent with a named trigger**: nothing has ever been upgraded, and the first release is the trigger. **What it costs to leave**: nothing today and everything at once later. A self-hoster's first `docker pull` of `0.1.0` is the first moment the version tags, the changelog, the migration note and the schema guard are all load-bearing together, and each of them is currently a sentence in a decision record | WP-22 (the five images and their tag scheme, which `image.yml` already implements), WP-23 (the operator guide the release notes point at, including the documented `pg_dump`-before-upgrade), **WP-33** (M2's remaining row — the nightly real-LLM smoke and the evals in CI; a 0.1.0 cut without it ships prompts no tier has ever measured against a model), and every other M3 row, because a release is the last thing in a milestone. **Blocked on human: none** — TD-019 decides the mechanism, and the monorepo versioning shape is an engineering choice this row makes and records. No daemon beyond what CI already runs | **(1) `release.yml` exists and does what technical/11:25 says**: release-please v5 on `push: main`, a release PR a human merges, a `vX.Y.Z` tag, and the tag reaching `image.yml`'s existing tag path — asserted by reading the workflow's own trigger, not by asserting it in prose (rule 3: an invariant stated in a comment is not evidence it holds). **(2) The versioning shape is decided and written down**: one product version or release-please `manifest` mode per package, with the reason, and the eleven `0.0.0` manifests moved together. **(3) `CHANGELOG.md` is generated from the conventional commits `verify:commits` already enforces**, and its first entry covers M1–M3 rather than starting at the release commit. **(4) The release notes state whether a migration is required** (TD-019), derived from the migration set on disk rather than written by hand — which is rule 7's shape: a hand-maintained scope drifts. **(5) `findUnknownMigrations` gets its caller and the refusal is asserted in both directions** (rule 42): a process whose database carries a migration this build does not know **refuses to start and names it**, and one whose database is at or behind the build's set starts — in the integration tier, against a real database, because the whole point of the rule is what happens to a real upgrade. **(6) TD-019's hygiene list is completed or each omission is named with its reason**: `CODE_OF_CONDUCT.md`, `.github/CODEOWNERS`, `.github/ISSUE_TEMPLATE/`. **(7) `THIRD_PARTY_NOTICES.md` is regenerated by a command in the repository**, or the change states that it is hand-maintained and what keeps it true (rule 7). **Needs measurement** (rule 66, not run by the refiner): whether the checked-in file matches the current dependency set was **not** checked here. **(8) technical/11's workflow table is corrected in the same change** (rule 83, docs win): three files exist, six of the eleven are `ci.yml` jobs, `evals.yml` and `nightly-llm.yml` are WP-33's, and `codeql.yml` and `mutation.yml` have no owner — filed as a finding rather than quietly dropped. **(9) Every `uses:` stays pinned to a SHA** and the ruleset's required checks are named (TD-019), which the lint job's actionlint and zizmor steps already enforce. **(10) product/14:29's MVP exit criteria are stated against the release and each is marked as evidenced or not**: they are **dogfooding** facts (*"20 real tickets … ≥ 60 % merged; cost per merged feature measured; ≥ 10 accepted KB proposals; zero secret leaks"*), not CI facts, and a release note that implied CI had established them would be the largest unlabelled hypothesis this project has published (rule 39) |

## Suggested agent team for the orchestrator
- **Architect (this role, read-only)** for questions about decisions; **Backend developer** (domain/application/infrastructure), **Integrations developer** (providers + contract tests), **Runner/security developer** (SDK runner, launcher, isolation), **Frontend developer** (SPA), **Prompt engineer** (roles, evals), **Reviewer** (fresh context, security checklist), **Docs writer**. Sequence M1 WPs by dependencies; WP-13 early because the whole runner path depends on it.

## Definition of done (per WP)
Tests per technical/10 green in CI; coverage thresholds met; contract/golden fixtures added; docs updated; no new `[unverified]` claims in docs without a TODO entry; reviewer approval; conventional commits with DCO.
